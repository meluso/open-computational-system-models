# Part One: An Inventory of Computational System Models

What follows is an inventory of computational system models, organized by domain and written to be read aloud. A computational system model, for our purposes, is a representation of an engineered system with many interacting parts that a person can actually run, so that changing inputs or parameters produces observably different outcomes. Every model named here meets that one test. You can vary something and watch a result change.

The inventory holds 89 models across 19 domains. We take the domains in turn.

We begin with executable Model-Based Systems Engineering, often shortened to M B S E (the practice of capturing a system as a connected model rather than as scattered documents). The models that interest us here do more than describe. They compute. They carry analysis and constraint definitions, so changing a parameter changes a calculated result.

In this domain the inventory records 4 models.

Consider Airbus Apollo 11 SysML v2 Model (an open SysML v2 (Systems Modeling Language version 2) reference model of the Apollo 11 lunar mission, including executable calc and analysis definitions such as delta-V and power-margin calculations). It represents the Apollo 11 lunar mission as a system-of-systems (Saturn V launch vehicle, Command/Service Module, Lunar Module, ground/mission elements), decomposed across purpose, operational, functional, logical, and technical layers using Airbus's CoSMA framework. A user can change stage mass and propellant parameters feeding the Tsiolkovsky calculateDeltaV calc def, power-consumption and supply values feeding the SystemPowerAnalysis, reliability values feeding MissionReliabilityAnalysis, cost values feeding MissionCostAnalysis, and structural decomposition / part definitions in the model packages. In response, the model reports computed delta-V budget per stage / mission segment, system power margin (surplus or deficit), rolled-up mission cost, and mission reliability figure. To run it you need Any standards-compliant SysML v2 tool able to parse and evaluate the textual notation (e.g. the SysML v2 Pilot Implementation Jupyter kernel). It is open source under a weak-copyleft license, released under the MPL-2.0 license. As of the last check it was actively maintained. The latest version on record is 0.0.1, from 2026-02-23. Suits MBSE practitioners and students learning SysML v2 on a non-trivial, real-world reference model; the layered structure and calc/analysis defs make it a useful executable benchmark rather than a beginner's first model.

Consider Eclipse Capella (an open-source Model-Based Systems Engineering (MBSE) tool implementing the Arcadia method, with add-ons that make mode/state machines executable and viewpoints that compute mass, cost, and latency). It represents arbitrary engineered systems (the Arcadia method targets complex systems and software architectures); a representative model spans operational, system, logical, and physical architecture layers with functions, components, exchanges, and state machines. A user can change mass and cost property values on components (Mass/Cost viewpoints), latency values along functional chains (Latency viewpoint), state-machine structure and triggers (DESS add-on), functional/component decomposition and exchanges, and simulation inputs/events driving the executable state machines. In response, the model reports rolled-up system mass and cost, end-to-end functional-chain latency, executed state-machine state sequence / recorded scenario, and simulation traces of mode/state transitions. To run it you need Capella workbench (Java / Eclipse RCP application) plus add-ons such as DESS (Dynamic Execution and System Simulation) and the basic Mass/Cost/Latency viewpoints. It is open source under a weak-copyleft license, released under the EPL-2.0 license. As of the last check it was actively maintained. The latest version on record is 7.0.1, from 2025-03-21. Suits systems engineers wanting a free, structured method-driven tool; the core is approachable for newcomers, while executable simulation and analysis viewpoints require installing add-ons and some method familiarity.

Consider Open-MBEE Thirty Meter Telescope SysML Model (A SysML (Systems Modeling Language) model of the Thirty Meter Telescope observatory with parametric analyses computing power, mass, and operational-duration roll-ups). It represents the Thirty Meter Telescope, an altitude-azimuth Ritchey-Chretien observatory with a 492-segment 30-meter primary mirror, active secondary, articulated tertiary, and an alignment-and-phasing wavefront-sensing system, along with its operational scenarios. A user can change per-component power draw values, per-component mass values, operational-state / scenario selection, durations of operational use cases, and parametric constraint equation parameters. In response, the model reports state-dependent power roll-up per operational scenario, total mass roll-up, operational use-case duration, and requirement satisfaction (pass/fail) against the design. To run it you need MagicDraw / Cameo Systems Modeler (Cameo 2022xR1) with the Model Development Kit (MDK). It is permissively open source, released under the Apache-2.0 license. As of the last check it was dormant but still usable. The latest activity on record is from 2023-09-12. Suits MBSE practitioners with access to Cameo/MagicDraw who want a real engineered-system example of executable parametric analysis; the commercial-tool dependency makes it less accessible to casual newcomers.

Consider SysML v2 Pilot Implementation (the reference pilot implementation of the SysML v2 (Systems Modeling Language version 2) textual notation, with a Jupyter kernel that evaluates expressions, calc defs, and constraints in a model). It represents arbitrary engineered systems modeled in SysML v2; a representative runnable model is an executable SysML v2 system (e.g. a vehicle or spacecraft) with dozens of part defs, calc defs, and constraint/analysis cases whose values are computed by the kernel. A user can change attribute/parameter values on parts and items, bindings and expressions in calc defs, constraint def parameters, analysis case inputs, and which expressions/queries are evaluated in the kernel. In response, the model reports evaluated numeric results of calc defs and expressions, constraint satisfaction (true/false) results, analysis case computed outputs, and validation results against SysML v2 rules. To run it you need Java plus JupyterLab 4.x with the SysML v2 Jupyter kernel (or Eclipse-based deployment). It is open source under a weak-copyleft license, released under the EPL-2.0 license. As of the last check it was actively maintained. The latest version on record is 2026-04, from 2026-05-13. Suits MBSE tool developers, standards followers, and advanced practitioners who want to actually evaluate SysML v2 models; the Jupyter/Java setup is heavier than a polished commercial tool for a newcomer.

Next we turn to model-based design in the MathWorks tools, chiefly Simulink (a block-diagram environment for simulating dynamic systems). Access here is usually through a campus or employer license, which we record plainly rather than treat as a reason to exclude.

In this domain the inventory records 11 models.

Consider Field-Oriented Control of PMSM Using Quadrature Encoder (A field-oriented control (FOC) algorithm that regulates the speed of a three-phase permanent magnet synchronous motor (PMSM) using rotor-position feedback from a quadrature encoder). It represents A three-phase PMSM drive with an inverter and a quadrature-encoder position sensor, controlled by a field-oriented (vector) control algorithm. The example includes simulation and deployable target models (for TI C2000 boards such as F28069M / F28379D LaunchPads) plus a host model for serial monitoring. A user can change motor parameters (from datasheet or parameter-estimation tool), reference speed command (via the host model), inverter parameters and ADC offset values, quadrature-encoder index offset and counts-per-slit settings, and current/speed PI controller gains. In response, the model reports motor speed (tracking the reference), estimated rotor position, d/q-axis and phase currents, and debug signals on the host-model Time Scope. To run it you need MATLAB + Simulink + Motor Control Blockset (+ Fixed-Point Designer; Embedded Coder and C2000 Microcontroller Blockset for deployment). It is reached through a campus or employer license, released under the Proprietary (MathWorks) license. As of the last check it was actively maintained. The latest version on record is R2026a, from 2026-04. Suits motor-control / power-electronics engineers; requires licensed MATLAB + Motor Control Blockset to run, and additional coder products to deploy to hardware.

Consider HEV P1 Reference Application (Powertrain Blockset) (A full Hybrid Electric Vehicle (HEV) in the P1 architecture (motor directly coupled to the engine crankshaft), with engine, transmission, battery, motor, and supervisory powertrain control). It represents A complete passenger-car hybrid electric powertrain in the P1 configuration, where the electric motor is mounted on the engine crankshaft, including an internal combustion engine, transmission, lithium-ion battery, motor, and the associated powertrain control algorithms. Used for HIL testing, tradeoff analysis, and control-parameter optimization. A user can change drive-cycle / driver scenario (reference velocity), environmental conditions (road grade, temperature, pressure), driver commands (acceleration, braking, gear), powertrain component sizing/parameters (Virtual Vehicle Composer), and supervisory energy-management controller settings. In response, the model reports vehicle speed and acceleration, battery state of charge (SOC), fuel economy and emissions, engine and motor power/torque split, and powertrain efficiency. To run it you need MATLAB + Simulink + Powertrain Blockset (Stateflow for controller logic). It is reached through a campus or employer license, released under the Proprietary (MathWorks) license. As of the last check it was actively maintained. The latest version on record is R2026a, from 2026-04. Suits powertrain/controls engineers with a Simulink background; requires licensed MATLAB + Powertrain Blockset to run.

Consider HEV P2 Reference Application (Powertrain Blockset) (A full Hybrid Electric Vehicle (HEV) in the P2 architecture (motor coupled between engine and transmission), with engine, battery, motor, dual-clutch transmission, and supervisory powertrain control). It represents A complete passenger-car hybrid electric powertrain in the P2 configuration, with a spark-ignition internal combustion engine, lithium-ion battery, electric traction motor, and dual-clutch transmission, plus the vehicle controller (VCU), engine controller (ECU), transmission controller (TCU), and braking control. It is used for fuel-economy tradeoff analysis, control calibration, and hardware-in-the-loop (HIL) testing. A user can change driver scenario / drive-cycle variant (reference velocity and maneuvers), environmental conditions (road grade, wind, temperature, pressure), driver commands (acceleration, braking, gear, steering), powertrain component sizing/parameters via the Virtual Vehicle Composer, and supervisory energy-management (ECMS) controller settings. In response, the model reports vehicle speed and performance metrics, battery state of charge (SOC), fuel economy and tailpipe emissions, component-level power and energy flows, and engine and powertrain efficiency (e.g. efficiency histograms). To run it you need MATLAB + Simulink + Powertrain Blockset (Stateflow used for controller logic). It is reached through a campus or employer license, released under the Proprietary (MathWorks) license. As of the last check it was actively maintained. The latest version on record is R2026a, from 2026-04. Suits powertrain/controls engineers with a Simulink background; requires licensed MATLAB + Powertrain Blockset to run. Not a newcomer's first model.

Consider Highway Lane Following (an Advanced Driver Assistance System (ADAS) test bench that steers a vehicle to stay in its lane and keep a safe distance, combining camera/radar sensor fusion, lane detection, decision logic, and control in a 3D simulation). It represents A highway lane-following automated-driving system integrating vision-based lane-marker and vehicle detectors, camera/radar forward-vehicle sensor fusion, lateral/longitudinal decision logic, and a model-predictive lane-following controller, simulated against a photorealistic Unreal Engine 3D scenario with synthetic camera and radar sensor models. A user can change test scenario (nine straight/curved road and traffic variants), set (desired cruising) velocity, camera intrinsics / lens parameters, and time-gap safety threshold (default 0.8 s). In response, the model reports lane boundary detections and lateral deviation from lane center, steering-angle commands, relative distance and relative velocity to the lead vehicle, acceleration/deceleration commands, and annotated video with overlaid detections. To run it you need MATLAB + Simulink + Automated Driving Toolbox + Model Predictive Control Toolbox + Sensor Fusion and Tracking Toolbox + Deep Learning Toolbox + Simulink 3D Animation. It is reached through a campus or employer license, released under the Proprietary (MathWorks) license. As of the last check it was actively maintained. The latest version on record is R2026a, from 2026-04. Suits ADAS / autonomy engineers comfortable with Simulink and multiple toolboxes; heavy product dependency and requires a licensed MATLAB install plus 3D simulation support. Not for newcomers.

Consider Microgrid Design with Simscape (File Exchange) (A File Exchange project of configurable microgrid models (remote, industrial, and techno-economic) with diesel generators, battery storage, photovoltaics, loads, protection, and supervisory control, built in Simscape Electrical). It represents standardized building-block microgrid models combining distributed energy resources (solar, wind, diesel generators), battery energy storage, protection logic, and supervisory/dispatch control with configurable topology. Three scenario models cover a remote microgrid, an industrial (dual-grid-connection) microgrid, and a techno-economic optimization study. A user can change microgrid topology / component sizing, operating scenario (feeder switch, diesel trip, planned islanding, black start, resynchronization), supervisory / energy-management dispatch settings, renewable resource profiles (solar, wind), and battery energy-storage parameters. In response, the model reports bus voltages and frequency, power flows among DERs, storage, and loads, diesel fuel consumption / renewable utilization, islanding and resynchronization transients, and techno-economic cost metrics. To run it you need MATLAB + Simulink + Simscape + Simscape Electrical + Stateflow + Optimization Toolbox + Parallel Computing Toolbox (project supports R2022b through R2025b; current FX version requires R2025b or later). It is reached through a campus or employer license, released under the BSD-style license with an added MathWorks-products-only restriction (clause 3); Copyright 2022 The MathWorks, Inc. (NOT pure BSD-3-Clause — files licensed solely for use with MathWorks products) license. As of the last check it was actively maintained. The latest version on record is 25.2.1.0, from 2026-03-02. Suits power-systems engineers and researchers; files are free to download from File Exchange/GitHub but require a licensed MATLAB plus several toolboxes to run.

Consider Model and Control a Manipulator Arm with Robotics and Simscape (A pick-and-place workflow for an ABB YuMi industrial robot arm, designing control in Simulink and simulating the multibody mechanics and contact with Simscape Multibody). It represents A single arm of the dual-arm ABB YuMi industrial manipulator executing a pick-and-place task, modeled across three increasing-fidelity models culminating in a high-fidelity Simscape Multibody mechanical simulation with contact proxies, a gripper, and an object (widget) to be grasped. A user can change waypoint configurations (robot joint positions), trajectory parameters (trapezoidal velocity profile), gripper open/close command, contact-model parameters (stiffness, damping, friction), and controller gains. In response, the model reports joint positions, velocities, and accelerations, gripper status and reaction/contact forces, object (widget) position and contact interactions, and task-scheduler state transitions. To run it you need MATLAB + Simulink + Robotics System Toolbox + Simscape + Simscape Multibody. It is reached through a campus or employer license, released under the Proprietary (MathWorks) license. As of the last check it was actively maintained. The latest version on record is R2026a, from 2026-04. Suits robotics / mechatronics engineers comfortable with Simulink and Simscape Multibody; requires licensed MATLAB plus those toolboxes.

Consider Modeling a Fault-Tolerant Fuel Control System (sldemo_fuelsys) (the classic Simulink/Stateflow model of a gasoline-engine fuel control system that detects individual sensor failures and dynamically reconfigures to keep running). It represents A fuel control system for a gasoline engine that computes air-fuel ratio (air mass flow divided by injected fuel mass flow, targeting stoichiometric 14.6:1) and remains robust to sensor faults: a single Stateflow chart with six parallel states monitors four sensors and reconfigures the fuel-injection mode (normal, rich, or shutdown) when failures occur. A user can change throttle angle (e.g. ramp 10-20 degrees), engine speed (normal vs high-speed), throttle-sensor fault injection (Dashboard switch), engine-speed-sensor fault injection, oxygen (EGO) sensor fault injection, and manifold-absolute-pressure (MAP) sensor fault injection. In response, the model reports fuel flow rate, air-fuel mixture ratio, system operating mode (normal / rich / disabled), and Stateflow state activity during faults. To run it you need MATLAB + Simulink + Stateflow. It is reached through a campus or employer license, released under the Proprietary (MathWorks) license. As of the last check it was actively maintained. The latest version on record is R2026a, from 2026-04. Suits newcomers learning Stateflow supervisory logic as well as controls engineers; only base Simulink + Stateflow needed (lower toolbox barrier than other entries), but still requires a licensed MATLAB.

Consider NASA HL-20 Lifting Body Airframe (A six-degree-of-freedom (6DOF) flight model of NASA's HL-20 lifting-body reentry vehicle airframe with its flight controller, a low-cost complement concept to the Space Shuttle orbiter). It represents the airframe and flight controller of the NASA HL-20 lifting body during the approach/reentry phase, modeled as a rigid-body 6DOF vehicle with aerodynamic coefficient lookup tables, environmental (atmosphere/gravity/wind) models, and control-surface actuation. The model assumes constant mass properties and lateral symmetry and excludes landing-gear effects. A user can change control-surface deflections (elevator, ailerons, body flaps, rudder), environmental conditions (altitude, wind shear, gusts, turbulence), vehicle state initialization, and aerodynamic coefficient / lookup-table data. In response, the model reports vehicle position and attitude (Euler angles), velocity and body angular rates, aerodynamic forces and moments, and flight-instrument indications. To run it you need MATLAB + Simulink + Aerospace Blockset. It is reached through a campus or employer license, released under the Proprietary (MathWorks) license. As of the last check it was actively maintained. The latest version on record is R2026a, from 2026-04. Suits aerospace GNC / flight-controls engineers and advanced students; requires licensed MATLAB + Aerospace Blockset (FlightGear/Unreal optional for visualization).

Consider Three-Phase Grid-Connected Solar Photovoltaic System (A grid-connected rooftop solar photovoltaic (PV) plant that feeds three-phase power into the grid at unity power factor, with maximum-power-point tracking (MPPT) and an inverter, built in Simscape Electrical). It represents A three-phase grid-connected solar PV system with no intermediate DC-DC converter: parallel strings of series-connected solar panels (modeled with the Solar Cell block), an MPPT controller, a DC-AC inverter, and a grid interface that injects solar power at unity power factor. A user can change solar irradiance and temperature, panel configuration (panels per string, parallel strings), MPPT technique (incremental conductance vs perturb-and-observe), and inverter type (average, two-level, or three-level). In response, the model reports real power delivered to the grid, maximum-power-point tracking performance (DC voltage/power), three-phase grid current/voltage and power factor, and small-signal stability metrics (phase and gain margin from Bode plots). To run it you need MATLAB + Simulink + Simscape Electrical. It is reached through a campus or employer license, released under the Proprietary (MathWorks) license. As of the last check it was actively maintained. The latest version on record is R2026a, from 2026-04. Suits power-systems / power-electronics engineers and advanced students; requires licensed MATLAB + Simscape Electrical.

Consider Use Model-Based Design to Build a Battery Management System (A complete model-based-design project for a Battery Management System (BMS) governing a 27 Ah Nickel-Manganese-Cobalt (NMC) lithium-ion battery pack, covering state estimation, protection, and cell balancing). It represents A lithium-ion battery pack built from 27 Ah NMC cells together with its full battery management system, developed through a model-based-design workflow with requirements, system architecture, plant model (Simscape Battery/Electrical), and BMS control logic (Stateflow) at unit, subsystem, and system levels. A user can change load / charge-discharge current profile, initial cell states of charge (to create imbalance), cell-balancing algorithm parameters (e.g. bleed-resistor sizing), protection thresholds (over/under voltage, current, temperature), and cell/pack chemistry and capacity parameters. In response, the model reports estimated cell/pack state of charge (SOC), cell voltages and balancing behavior over time, protection / fault state transitions, and pack current and temperature response. To run it you need MATLAB + Simulink + Simscape + Simscape Battery + Simscape Electrical + Stateflow + System Composer + Requirements Toolbox + Simulink Test. It is reached through a campus or employer license, released under the Proprietary (MathWorks) license. As of the last check it was actively maintained. The latest version on record is R2026a, from 2026-04. Suits battery-systems and controls engineers running a full MBD toolchain; requires a licensed MATLAB plus many toolboxes. Substantial, not a starter example.

Consider Vehicle Dynamics, 14 DOF Model in Simscape Multibody (File Exchange) (A File Exchange 14-degree-of-freedom (DOF) full-vehicle dynamics model in Simscape Multibody, with heave and roll at front and rear axles and a multibody tire model, exercised by several driving maneuvers). It represents A full passenger vehicle modeled in Simscape Multibody with 14 degrees of freedom (including heave and roll at both front and rear axles) and the Simscape Multibody Tire model, used to study ride and handling across maneuvers and road/surface conditions. A user can change driving maneuver (step steer, plateau, parking lot, slalom on a hill, rough road, icy surface), road surface / friction conditions, steering input profile, vehicle speed and braking input, and suspension and tire parameters. In response, the model reports vehicle lateral dynamics (yaw, roll), body heave/pitch/roll motion, response to road slope and rough/icy surfaces, and braking and low-speed/reverse behavior. To run it you need MATLAB + Simulink + Simscape + Simscape Multibody (versions provided for R2022a through R2025b). It is reached through a campus or employer license, released under the BSD-style license with an added MathWorks-products-only restriction (clause 3); Copyright 2021-2022 The MathWorks, Inc. (NOT pure BSD-3-Clause — files licensed solely for use with MathWorks products) license. As of the last check it was actively maintained. The latest version on record is 26.1.1.3, from 2026-05-16. Suits vehicle-dynamics engineers and students; files are free to download from File Exchange/GitHub (author Steve Miller, MathWorks) but require a licensed MATLAB + Simscape Multibody to run.

Now consider multidisciplinary design optimization, shortened to M D O (the practice of coupling several engineering disciplines and letting an optimizer trade them off against one another). These frameworks turn a whole vehicle into something you can optimize.

In this domain the inventory records 6 models.

Consider Dymos (open-source framework for optimal control and trajectory optimization of dynamical systems, built on the OpenMDAO (OpenMDAO = open-source Multidisciplinary Design Analysis and Optimization) framework). It represents models time-evolving engineered dynamical systems (e.g. aircraft and spacecraft trajectories, electric powertrains, chemical processes) as ordinary-differential-equation systems and solves optimal control problems over them using pseudospectral transcription. Couples the dynamics ODE to design/optimization variables so trajectories can be optimized subject to constraints. A user can change ODE state and control variable definitions for the dynamic system, Phase transcription type and number of segments/collocation nodes, Boundary and path constraints (initial/final states, bounds), Objective function (e.g. minimize time, fuel, energy), and Design parameters fed to the underlying OpenMDAO model. In response, the model reports Optimized state and control time histories, Objective value (e.g. minimum time-to-climb, minimum fuel), Constraint satisfaction / collocation defect residuals, Phase timing and event values, and Derivatives/sensitivities of outputs to design variables. To run it you need Python 3 with OpenMDAO; installed via pip ("dymos"); a supported optimizer (SciPy SLSQP, or pyOptSparse/SNOPT/IPOPT) recommended. It is permissively open source, released under the Apache-2.0 license. As of the last check it was actively maintained. The latest version on record is 1.15.1, from 2026-03-12. Researchers and engineers in aerospace and other domains doing trajectory optimization and optimal control coupled to multidisciplinary design models.

Consider NASA Aviary (NASA's open-source aircraft analysis, design, and optimization tool built on the OpenMDAO (open-source Multidisciplinary Design Analysis and Optimization) framework). It represents models a complete fixed-wing aircraft for conceptual and preliminary multidisciplinary design and optimization, integrating mass/sizing, aerodynamics, propulsion, and mission/trajectory analysis. Incorporates sizing methods from NASA's legacy GASP (General Aviation Synthesis Program) and FLOPS (Flight Optimization System) tools. A user can change Aircraft geometry and sizing parameters (wing area, aspect ratio, etc.), Mission profile and segment definitions, Propulsion/engine model selection and parameters, Aerodynamics and mass-estimation method choices, and Design variables and optimization objectives/constraints. In response, the model reports Gross/empty weight and component mass breakdown, Fuel burn and mission performance, Aerodynamic coefficients across the mission, Optimized design variable values, and Trajectory/mission state histories. To run it you need Python 3 with OpenMDAO; installed via pip ("aviary"); optimizer such as SciPy or pyOptSparse recommended. It is permissively open source, released under the Apache-2.0 license. As of the last check it was actively maintained. The latest version on record is 1.0, from 2026-04-29. Aircraft conceptual/preliminary designers, university senior-design courses, NASA and industry MDO practitioners.

Consider OpenMDAO (an open-source framework for gradient-based multidisciplinary design optimization of coupled engineering systems). It represents coupled multidisciplinary engineering systems — for example an aircraft whose aerodynamics, structures, and propulsion are optimized together — assembled from interconnected analysis components. A user can change design variables (e.g. geometry, sizing, operating conditions), constraints and objective function, component connections and model topology, solver and optimizer settings, and analytic vs. finite-difference derivative options. In response, the model reports optimized design variable values, objective and constraint values at the optimum, coupled multidisciplinary analysis results, total derivatives / design sensitivities, and optimizer iteration history. To run it you need Python 3 (with NumPy/SciPy; optional pyOptSparse for advanced optimizers). It is permissively open source, released under the Apache-2.0 license. As of the last check it was actively maintained. The latest version on record is 3.43.0, from 2026-03-11. Suits engineers and researchers comfortable with Python who need rigorous, derivative-based optimization of coupled systems; steeper learning curve than a point tool, so less suited to a casual newcomer.

Consider pyCycle (thermodynamic gas-turbine engine cycle modeling library built on the OpenMDAO (open-source Multidisciplinary Design Analysis and Optimization) framework). It represents models the thermodynamic cycle of gas turbine / jet engines (turbofans, turbojets, etc.) by assembling components such as inlets, compressors, burners, turbines, and nozzles with chemical-equilibrium thermodynamics, enabling on- and off-design cycle analysis and gradient-based optimization. A user can change Engine architecture / component connectivity (inlet, compressor, burner, turbine, nozzle), Component design parameters (pressure ratios, efficiencies, bleed/cooling flows), Flight condition (Mach number, altitude) and design point, Fuel and combustor settings, and Design variables and optimization objectives. In response, the model reports Net thrust and specific fuel consumption, Station temperatures, pressures, and mass flows, Component work and efficiency, Overall pressure ratio and bypass ratio, and Sensitivities/derivatives for optimization. To run it you need Python 3 with OpenMDAO; cloned from GitHub (OpenMDAO/pyCycle); optimizer such as SciPy or pyOptSparse recommended. It is permissively open source, released under the Apache-2.0 license. As of the last check it was actively maintained. The latest version on record is 4.4.0, from 2025-10-15. Propulsion engineers and researchers doing engine cycle analysis and engine/airframe co-optimization.

Consider RCAIDE (RCAIDE (Research Community Aerospace Interdisciplinary Design Environment) is an open-source Python platform for aircraft design, analysis, and optimization, serving as the modernized successor to SUAVE). It represents models complete aircraft (from commercial airliners to UAVs and hybrid-electric designs) for interdisciplinary conceptual/preliminary design, integrating mission analysis, aerodynamics, propulsion/energy networks, weights estimation, and performance evaluation with validated engineering methods. A user can change Vehicle geometry and configuration, Energy/propulsion network and component parameters (fuel, electric, hybrid), Mission profile and segment definitions, Analysis method/fidelity selection, and Optimization design variables, objectives, and constraints. In response, the model reports Weight and mass-component breakdown, Mission fuel/energy use, Aerodynamic performance (lift, drag, L/D), Mission/performance state histories, and Optimized design variable values. To run it you need Python 3; installed via pip ("RCAIDE-LEADS") or cloned from GitHub (leadsgroup/RCAIDE_LEADS). It is open source under a copyleft license, released under the AGPL-3.0 license. As of the last check it was actively maintained. The latest version on record is 1.2.1, from 2025-04-10. Aircraft conceptual/preliminary designers and researchers, including those migrating from SUAVE and working on electric/hybrid configurations.

Consider SUAVE (SUAVE (Stanford University Aerospace Vehicle Environment) is an open-source Python aircraft conceptual design and analysis toolbox supporting conventional and unconventional configurations). It represents models a complete aircraft for conceptual design and mission analysis, integrating geometry, aerodynamics, propulsion (including electric and hybrid), weights, and mission/energy networks, enabling multi-fidelity analysis and optimization across diverse vehicle configurations. A user can change Vehicle geometry and configuration definition, Propulsion/energy network selection and parameters (turbofan, electric, hybrid), Mission profile and segments, Aerodynamics and weights method/fidelity selection, and Design variables and optimization setup. In response, the model reports Aircraft weight breakdown, Fuel/energy consumption over the mission, Aerodynamic performance (lift, drag, L/D), Mission state histories (altitude, speed, range), and Optimized design variables. To run it you need Python with the SUAVE package; cloned from GitHub (suavecode/SUAVE). It is open source under a weak-copyleft license, released under the LGPL-2.1 license. As of the last check it was dormant but still usable. The latest version on record is 2.5.2, from 2022-03-17. Aircraft conceptual designers and researchers; note that new users are generally directed toward its successor RCAIDE.

We move to equation-based physical modeling. The idea is to write the physics as equations and let a solver sort out cause and effect, rather than fixing the direction of computation in advance. Modelica is the central open language in this tradition.

In this domain the inventory records 7 models.

Consider Dymola (commercial Modelica-based environment (Dynamic Modeling Laboratory) for modeling and simulating integrated multi-domain physical systems). It represents dymola (Dassault Systemes) models complex engineered systems combining mechanical, electrical, electronic, hydraulic, thermal, control, and electric-power components in a single Modelica system model—any physical component describable by ordinary differential and algebraic equations. Common in automotive, aerospace, and robotics. A user can change Component parameters within the drag-and-drop model, Variables for parameter sweeps across multiple simulations, Model topology and custom component designs, Simulation settings (solver, time span), and Input/boundary signal sources. In response, the model reports Simulation results as 2D or 3D plots, System behavior under different scenarios, Time trajectories of model variables, and Hardware-in-the-loop validation results. To run it you need Dymola (commercial desktop application, Windows/Linux). It is commercially licensed, though the exact license is unstated and needs direct verification. As of the last check it was actively maintained. Industrial engineers needing a high-performance commercial Modelica environment with strong solver and library ecosystem.

Consider GT-SUITE (commercial multi-physics system simulation platform from Gamma Technologies for engine, vehicle, and broader system modeling). It represents GT-SUITE (Gamma Technologies) is an enterprise multi-physics platform built on physics libraries for flow, acoustics, thermal, mechanical, electrical/electromagnetic, chemistry, controls, and 3D CFD/FE. Applications include engines, vehicles, batteries, fuel cells, heat exchangers, and mechanical systems across the product-development cycle. A user can change Design parameters within optimization/DOE studies, Component/library specifications and geometry, Operating and boundary conditions, Control signals and maps, and Input distributions for Monte Carlo / variability studies. In response, the model reports Performance predictions (e.g., engine power, efficiency), System behavior under varied conditions, Sensitivity and optimization results, Variability/Monte Carlo output distributions, and Time-series of flow/thermal/mechanical variables. To run it you need GT-SUITE (commercial desktop application). It is commercially licensed, though the exact license is unstated and needs direct verification. As of the last check it was actively maintained. Powertrain, vehicle, and thermal/fluid system engineers needing detailed multi-physics system simulation and optimization.

Consider MapleSim (commercial multidomain system-level modeling and simulation platform from Maplesoft, with symbolic model generation). It represents MapleSim (Maplesoft) is a system-level modeling and simulation tool for multidomain machine design, combining mechanical, electrical, control, robotics, vehicle-dynamics, and web-handling components into virtual prototypes. It integrates with PLC systems for virtual commissioning and digital-twin applications. A user can change Component parameters within the model diagram, System topology / component connections, Control laws and signal inputs, Simulation/solver settings, and Boundary and operating conditions. In response, the model reports Time-series simulation results of model variables, System dynamic behavior across scenarios, Real-time / HIL simulation responses, and Exported FMU and C-code behavior. To run it you need MapleSim (commercial desktop application). It is commercially licensed, though the exact license is unstated and needs direct verification. As of the last check it was actively maintained. Engineers wanting symbolic system modeling, plant models for control design, and FMU/code export for virtual commissioning.

Consider Modelica Buildings Library (free Modelica library from Lawrence Berkeley National Laboratory for modeling building energy and control systems, district energy, and HVAC). It represents the Modelica Buildings Library (LBNL) provides Modelica components for whole-building energy and control simulation—HVAC equipment, fluid loops, heat exchangers, controls (including ASHRAE Guideline 36 sequences), and district heating/cooling networks. Modelers assemble these into runnable building-system models. A user can change Equipment sizing and component parameters (e.g., chiller capacity, fan curves), Control sequence parameters and setpoints, Weather/boundary input files, Fluid loop topology and connections, and Initial conditions and simulation horizon. In response, the model reports Building/system energy use time-series, Zone and fluid temperatures, Equipment power and flow rates, Control signal trajectories, and Heat-exchanger and loop performance. To run it you need A Modelica tool (OpenModelica, Dymola); library plus optional BuildingsPy/Spawn tooling. It is permissively open source, released under the BSD 3-Clause license. As of the last check it was actively maintained. The latest version on record is 13.0.0, from 2026-05-04. Building-energy researchers and engineers, control-sequence developers, and district-energy modelers.

Consider Modelica Standard Library (MSL) (free component library of reusable physical and control building blocks used to assemble runnable Modelica system models). It represents the Modelica Standard Library (MSL) is the canonical free library of acausal physical components—electrical, mechanical (translational/rotational/multibody), thermal, fluid, magnetic—plus blocks and math. A modeler wires these components inside any Modelica tool to construct a runnable system model (e.g., a motor-drive, an HVAC loop, a vehicle drivetrain). A user can change Parameters of each instantiated component (e.g., resistance, inertia, heat capacity), Source/boundary blocks (voltage, force, temperature, flow inputs), Connections/topology among components, Initial values of component states, and Choice of components/variants from the library. In response, the model reports Potential/flow variables at each connector (voltages, currents, forces, flows), Time-series of component state variables, Power and energy balances across the assembled system, and Transient and steady-state system response. To run it you need Any Modelica-compliant tool (OpenModelica, Dymola, etc.); MSL is a library, not a standalone runtime. It is permissively open source, released under the BSD 3-Clause (MSL itself); documentation under CC-BY license. As of the last check it was actively maintained. The latest version on record is 4.1.0, from 2025-05-23. Anyone building Modelica system models; foundational dependency for OpenModelica, Dymola, and other tools.

Consider OpenModelica (open-source Modelica-based modeling and simulation environment for building and running multi-domain physical system models). It represents A full integrated environment (compiler, solvers, OMEdit graphical editor) for equation-based modeling of engineered physical systems—electrical, mechanical, thermal, fluid, and control—described as differential-algebraic equations. Users wire together library components (e.g., the Modelica Standard Library) into system models and simulate their dynamics. A user can change Component parameters (resistances, masses, gains, time constants), Initial conditions of state variables, Simulation interval, step size, and solver/integrator selection, Model topology (wiring of components in OMEdit), and Input signal sources (steps, ramps, tables). In response, the model reports Time-series trajectories of any model variable (plotted in OMEdit), State variable evolution over the simulation horizon, Derived energy/power flows between components, Event timings (state and time events), and Steady-state and transient response curves. To run it you need OpenModelica (OMEdit / OMC compiler); cross-platform desktop application. It is open source under a copyleft license, released under the OSMC-PL (OpenModelica) / GPL dual; see project license license. As of the last check it was actively maintained. The latest version on record is 1.26.7, from 2025-05-02. Engineers, researchers, and students wanting a free environment for system-level physical modeling without a commercial Modelica license.

Consider Simcenter Amesim (commercial 1D multi-domain mechatronic system simulation platform from Siemens for virtually assessing and optimizing system performance). It represents simcenter Amesim (Siemens) is a 1D multi-domain system-simulation platform covering fluid (hydraulic/pneumatic), mechanical, electrical/electromechanical, thermal, and propulsion systems. Engineers assemble validated component libraries into system models for powertrain, thermal management, actuation, and vehicle energy studies before building physical prototypes. A user can change Component specifications/parameters from physical libraries, System configuration and topology, Control laws, Environmental / operating conditions, and Input signal and boundary sources. In response, the model reports Dynamic behavior time-series of system variables, Energy flows (graphically visualized), Power generation and consumption metrics, Emissions and performance assessments, and Optimization results. To run it you need Simcenter Amesim (commercial desktop application); supports FMI and Simulink co-simulation. It is commercially licensed, though the exact license is unstated and needs direct verification. As of the last check it was actively maintained. Industrial system engineers in automotive/aerospace doing 1D multi-domain performance and energy analysis with MIL/SIL/HIL workflows.

Here are the controls and dynamics libraries written for Python. They let an engineer design, simulate, and optimize control systems in a familiar scripting language.

In this domain the inventory records 4 models.

Consider CasADi (open-source symbolic framework for algorithmic differentiation and numerical optimization, widely used to build nonlinear optimal-control and model-predictive-control problems). It represents dynamic engineered systems posed as optimal-control problems, e.g., robot trajectory optimization, vehicle path planning, or chemical reactor control. Users symbolically express ODE/DAE dynamics, constraints, and cost functions, then solve them with integrated NLP and integrator solvers. A user can change Symbolic system dynamics (ODE/DAE equations) and number of states/controls, Cost (objective) function and weighting terms, Path and boundary constraints and variable bounds, Discretization scheme and horizon length (shooting/collocation intervals), and Solver choice and options (e.g., IPOPT, SQP) and initial guesses. In response, the model reports Optimal state and control trajectories, Objective/cost value at the solution, Constraint and Lagrange-multiplier values, Solver convergence status and iteration counts, and Sensitivities/derivatives via automatic differentiation. To run it you need Python (or C++, MATLAB/Octave); pip install casadi; bundles IPOPT and SUNDIALS. It is open source under a weak-copyleft license, released under the LGPL-3.0-or-later license. As of the last check it was actively maintained. The latest version on record is 3.7.2, from 2025-09-10. Researchers and engineers formulating nonlinear optimization, optimal control, and MPC problems.

Consider do-mpc (python toolbox for nonlinear and robust Model Predictive Control (MPC) and Moving Horizon Estimation (MHE), built on the CasADi symbolic framework). It represents nonlinear dynamic engineered systems controlled in closed loop, e.g., chemical reactors, batteries, building HVAC, or robotic systems. Users define the plant model, an MPC controller, an estimator, and a simulator and run the closed-loop interaction. A user can change Model equations, states, inputs, and uncertain parameters, Prediction and control horizon lengths and time step, Objective terms (stage/terminal cost) and input penalties, Constraints and bounds on states/inputs, and Robust multi-stage scenario tree and uncertainty values. In response, the model reports Closed-loop state and control trajectories over time, Predicted (open-loop) trajectories at each step, Cost/objective evolution, Estimated states from the moving-horizon estimator, and Constraint satisfaction/violation behavior. To run it you need Python 3 with CasADi (pip install do-mpc); IPOPT solver. It is open source under a weak-copyleft license, released under the LGPL-3.0 license. As of the last check it was actively maintained. The latest version on record is 5.1.1, from 2025-10-31. Control researchers and engineers implementing nonlinear/robust MPC and state estimation.

Consider GEKKO (python package for machine learning and dynamic optimization of differential-algebraic-equation (DAE) systems, including model predictive control and nonlinear/mixed-integer programming). It represents dynamic engineered systems formulated as optimization or control problems, e.g., process control loops, energy systems, and chemical processes. Users declare variables, equations, and an objective, and GEKKO solves simulation, estimation, or control modes via large-scale solvers. A user can change Model equations and DAE dynamics, Variable bounds, manipulated/controlled variable tuning, and objective terms, Solver mode (steady-state, dynamic simulation, estimation, control), Time horizon and discretization (collocation nodes), and Solver selection (e.g., APOPT, IPOPT) and options. In response, the model reports Optimized variable and control trajectories over time, Objective function value, Parameter/state estimates, Solver status and convergence diagnostics, and Predicted plant responses for MPC. To run it you need Python 3 (pip install gekko); uses APMonitor backend solvers. It is permissively open source, released under the MIT license. As of the last check it was actively maintained. The latest version on record is 1.3.2, from 2025-12-31. Engineers and researchers doing dynamic optimization, parameter estimation, and MPC in Python.

Consider Python Control Systems Library (python library for analysis and design of feedback control systems (linear time-invariant systems, frequency response, state-space and transfer-function models)). It represents feedback control systems such as motor speed controllers, aircraft pitch dynamics, or chemical-process loops. Users build interconnected block diagrams of plants, sensors, and controllers as state-space or transfer-function models and analyze closed-loop behavior. A user can change State-space matrices (A, B, C, D) or transfer-function numerator/denominator coefficients, Controller gains (e.g., PID terms, LQR weighting matrices Q and R), Reference/input signals and simulation time vector for time-response functions, Interconnection/feedback topology via append, connect, feedback, and Sampling time for discrete-time system conversion. In response, the model reports Step, impulse, and forced time responses, Bode, Nyquist, and root-locus plots, Pole/zero locations and stability margins (gain/phase margin), Closed-loop frequency response and bandwidth, and Controllability/observability and computed optimal gains. To run it you need Python 3 with NumPy, SciPy, and Matplotlib (optional SLICOT/slycot for some routines). It is permissively open source, released under the BSD 3-Clause license. As of the last check it was actively maintained. The latest version on record is 0.10.2, from 2025-07-05. Control engineers, researchers, and students doing classical and modern control design and analysis in Python.

Now we reach discrete-event and agent-based simulation. In discrete-event simulation, a system jumps from one event to the next, such as a part arriving at a machine. In agent-based simulation, many individual agents follow simple rules and the system behavior emerges from their interaction.

In this domain the inventory records 8 models.

Consider AnyLogic (commercial multi-method simulation platform supporting discrete-event, agent-based, and system-dynamics modeling, with a free Personal Learning Edition (PLE)). It represents large-scale operational systems including supply chains, manufacturing, transportation networks, pedestrian/crowd dynamics, healthcare, and markets, modeled with agents, process flows, and stock-and-flow dynamics. A user can change Number and behavior rules of agents, Process-flow block parameters (capacities, delays, resources), Arrival rates and demand/input distributions, System-dynamics stocks, flows, and feedback parameters, and Experiment settings (optimization, parameter variation, Monte Carlo), run length, seed. In response, the model reports Throughput, utilization, and queue/wait statistics, Agent population dynamics over time, Stock/flow level trajectories, Spatial/animated state and dashboards, and Optimization and parameter-sweep results. To run it you need AnyLogic desktop application (Java-based); Windows/macOS/Linux. It is free in a limited tier with paid upgrades, released under the Proprietary (free Personal Learning Edition; paid Professional/University editions) license. As of the last check it was actively maintained. The latest version on record is 8.9.8, from 2026-02-26. Modelers and consultants needing a professional multi-method GUI simulation tool; PLE suits students and learners.

Consider Eclipse SUMO (simulation of Urban MObility (SUMO), an open-source microscopic, multi-modal traffic simulation suite for modeling individual vehicles, pedestrians, and public transport on road networks). It represents urban and highway road traffic, where each vehicle is simulated individually with car-following, lane-changing, and intersection/traffic-light logic across a city-scale network of streets and junctions. A user can change Road network topology (edges, lanes, junctions) and traffic-light programs, Vehicle demand (routes, flows, departure times) and number of vehicles, Vehicle types and car-following/lane-change model parameters, Simulation step length and total duration, and Random seed and routing/rerouting settings. In response, the model reports Per-vehicle and aggregate travel times and delays, Edge/lane flows, densities, and mean speeds, Queue lengths and congestion at intersections, Emissions and fuel-consumption estimates, and Trip/route statistics and time-series detector outputs. To run it you need SUMO suite (C++ binaries with Python TraCI/libsumo bindings); pip install eclipse-sumo; Windows/macOS/Linux. It is open source under a copyleft license, released under the EPL-2.0 OR GPL-2.0-or-later license. As of the last check it was actively maintained. The latest version on record is 1.27.0, from 2026-05-21. Traffic engineers and researchers simulating road networks, traffic control, and connected/automated vehicles.

Consider JaamSim (free, open-source Java-based discrete-event simulation (DES) software with a drag-and-drop graphical interface and 3D visualization for building object-oriented simulation models). It represents operational and logistics systems such as warehouses, mining haul networks, manufacturing plants, and supply chains, built from interconnected processing objects (servers, queues, conveyors, entity generators). A user can change Entity generation rates and interarrival distributions, Server/processing capacities and service-time distributions, Object connections and routing logic in the model flow, Resource quantities and queue priorities, and Simulation run duration, replications, and random seed. In response, the model reports Queue lengths and waiting times, Server/resource utilization, Entity throughput and cycle times, Time-series and statistical outputs (graphs, tables), and 3D animated state of the system. To run it you need Java (Java Runtime Environment); standalone desktop application (Windows/macOS/Linux). It is permissively open source, released under the Apache-2.0 license. As of the last check it was actively maintained. The latest version on record is 2026-03, from 2026-04-27. Industrial engineers and operations analysts who want a free GUI-based DES tool without coding.

Consider MATSim (Multi-Agent Transport Simulation (MATSim), an open-source Java framework for large-scale activity-based, agent-based transport simulation with co-evolutionary plan optimization). It represents whole-day travel demand and traffic in a city or region, where each synthetic traveler executes a daily activity-and-trip plan on a road/transit network, iteratively adjusting routes, departure times, and mode choices. A user can change Synthetic population size and agent daily plans (activities, trips), Network supply (links, capacities, free-speed) and transit schedules, Scoring/utility function parameters (value of time, mode constants), Replanning strategies and their weights (reroute, time mutation, mode choice), and Number of co-evolutionary iterations and random seed. In response, the model reports Link traffic volumes and congestion patterns over the day, Per-agent scores and travel times, Mode share and departure-time distributions, Convergence of average scores across iterations, and Event logs and visualizable network states (via VIA/SimWrapper). To run it you need Java (JVM); run from JAR/Maven; large scenarios benefit from substantial RAM. It is open source under a copyleft license, released under the GPL-2.0 license. As of the last check it was actively maintained. The latest version on record is MATSim 2026 (Spring), from 2026-04-10. Transport planners and researchers running large-scale activity-based travel-demand and traffic simulations.

Consider Mesa (apache-licensed Python framework for agent-based modeling (ABM), providing agent schedulers, spatial grids/networks, data collection, and browser-based visualization). It represents complex adaptive systems of many interacting agents such as epidemics, traffic, economic markets, segregation, and ecological models, built from dozens-to-thousands of agents on grids or networks. A user can change Number of agents in the model, Agent behavior rule parameters and thresholds, Space type and size (grid dimensions, network topology), Scheduling/activation order, and Number of simulation steps and random seed. In response, the model reports Agent and model-level variables collected over steps (DataCollector), Spatial visualization of agent positions/states, Time-series plots and charts in the browser interface, Batch-run parameter-sweep results as DataFrames, and Aggregate metrics (counts, means) per step. To run it you need Python 3.12+ (pip install mesa); Solara/browser for visualization. It is permissively open source, released under the Apache-2.0 license. As of the last check it was actively maintained. The latest version on record is 3.5.1, from 2026-03-15. Researchers, students, and data scientists building and analyzing agent-based models in Python.

Consider NetLogo (free, multi-agent programmable modeling environment for agent-based modeling (ABM), where "turtles" (agents) act on a grid of "patches" with links between them). It represents complex systems of many interacting agents such as traffic flows, epidemics, flocking, predator-prey ecosystems, segregation, and market dynamics, built from hundreds-to-thousands of agents following simple rules. A user can change Number of agents (turtles) via sliders, Behavioral rule parameters (speeds, thresholds, probabilities), World/grid size and topology (wrapping, patch count), Global parameters exposed as sliders/switches/choosers, and Random seed and tick (time-step) count. In response, the model reports Animated spatial view of agents and patches, Plots and monitors of aggregate quantities over time, Population counts and emergent patterns, BehaviorSpace experiment data tables, and Per-agent and per-patch reported variables. To run it you need NetLogo desktop (JVM-based, Java/Scala); also NetLogo Web; Windows/macOS/Linux. It is open source under a copyleft license, released under the GPL (GNU General Public License); commercial licenses also available license. As of the last check it was actively maintained. The latest version on record is 7.0.4, from 2026-04. Educators, students, social scientists, and complexity researchers building agent-based models.

Consider Repast4Py (python member of the Repast Suite of agent-based modeling (ABM) tools, designed for building distributed, large-scale agent-based simulations that run across multiple CPU cores via MPI on high-performance computing systems). It represents large-scale complex systems of many interacting agents such as disease spread, urban dynamics, or social networks, distributed across multiple processes so the agent population and space are partitioned over HPC cores. A user can change Number of agents and their behavioral parameters, Space type and dimensions (grids, continuous space, networks) and partitioning, Number of MPI ranks/processes for distribution, Number of simulation time steps and scheduling, and Random seed and model-specific parameters (via input config). In response, the model reports Aggregate and per-agent metrics logged over time, Agent spatial distributions and movement, Network/contact structures and counts, Time-series of model-level reporters, and Scaling behavior across processes. To run it you need Python 3 with MPI (mpi4py) and a C/C++ MPI library; pip install repast4py; uses NumPy/Numba/PyTorch. It is permissively open source, released under the BSD-3-Clause license. As of the last check it was actively maintained. The latest version on record is 1.2.1, from 2025-11-12. Computational social scientists and researchers needing scalable, parallel agent-based models on HPC.

Consider SimPy (process-based discrete-event simulation (DES) framework in pure Python, using generator functions to model active components competing for shared resources). It represents operational systems with queueing and resource contention, e.g., a manufacturing line, a hospital emergency department, a call center, or a car wash with dozens of servers, machines, and concurrent process flows. A user can change Number and capacity of resources (servers, machines, beds), Arrival/interarrival time distributions and rates, Service/processing time distributions, Number of entities/processes and simulation duration, and Random seed and process routing logic. In response, the model reports Entity wait times and time-in-system, Resource utilization and queue lengths over time, Throughput / number of entities served, Event timestamps and process traces, and Distribution statistics across replications. To run it you need Python 3 (pip install simpy); pure Python, no dependencies. It is permissively open source, released under the MIT license. As of the last check it was actively maintained. The latest version on record is 4.1.2, from 2026-05-24. Operations researchers, industrial engineers, and analysts modeling queueing and resource systems in Python.

We turn to power and energy systems. These models represent electrical grids and energy systems, from the flow of power through a distribution feeder to the expansion of a national energy system over decades.

In this domain the inventory records 10 models.

Consider Calliope (calliope, a multi-scale energy systems modeling framework for building and optimizing energy supply and demand models). It represents energy systems represented as a set of technologies (supply, demand, storage, conversion, transmission) at nodes connected by links, used to find least-cost (or other objective) capacity and operation across high-resolution time series. A user can change technology definitions, costs, and efficiencies (YAML), node and link (transmission) topology, demand and resource (e.g. renewable) time series, objective function and constraints (e.g. emissions caps), and spatial/temporal resolution. In response, the model reports optimal installed capacity per technology and node, energy flows and dispatch over time, storage state of charge, total system cost, and import/export and curtailment. To run it you need Python 3 (pip install calliope); an LP/MILP solver (GLPK, CBC, Gurobi, etc.). It is permissively open source, released under the Apache-2.0 license. As of the last check it was actively maintained. The latest version on record is 0.6.10 (stable); 0.7.0.dev7 (development), from 2023-01-18 (0.6.10 stable); 2025-09-07 (0.7.0.dev7). Energy system researchers and students building custom multi-scale optimization models with flexible configuration.

Consider EnergyPlus (EnergyPlus, the US Department of Energy's whole-building energy simulation engine for modeling heating, cooling, lighting, ventilation, and energy/water use). It represents whole buildings and their energy systems, modeled as thermal zones with surfaces, HVAC (heating, ventilation, and air-conditioning) systems, plant loops, lighting, and equipment, simulated against weather data using sub-hourly heat-balance physics. A user can change building geometry, zones, and construction/material properties, HVAC system and plant configuration and setpoints, weather file (location/climate), internal loads (occupancy, lighting, equipment) and schedules, and simulation timestep and run period. In response, the model reports zone air temperatures and comfort metrics, heating/cooling energy and peak loads, total site/source energy use by end use, HVAC equipment energy and water consumption, and surface heat flows and unmet load hours. To run it you need EnergyPlus engine (Windows/macOS/Linux); IDF/epJSON input files. It is permissively open source, released under the BSD-style (with US DOE government rights and trademark clause) license. As of the last check it was actively maintained. The latest version on record is v26.1.0, from 2026-03-31. Building energy modelers, mechanical engineers, and researchers performing detailed whole-building energy analysis and code compliance.

Consider GridLAB-D (GridLAB-D, the US Department of Energy's agent-based power distribution system simulation and analysis tool). It represents electric power distribution systems with detailed end-use models, modeled as interconnected objects (nodes, links, transformers, houses, loads, controls) using an agent-based, time-series simulation engine that couples power flow with end-use, market, and control behavior. A user can change feeder topology and component definitions (.glm models), house/thermal and end-use load parameters, weather/climate input data, DER, controller, and market module settings, and simulation timestep and duration. In response, the model reports node voltages and line/transformer power flows over time, aggregate and per-house load profiles, transformer/equipment loading and losses, controller actions and market clearing results, and voltage regulation and demand-response effects. To run it you need GridLAB-D executable (Windows/Linux/macOS builds); models written in GLM. It is permissively open source, released under the BSD-style (DOE/PNNL) license. As of the last check it was actively maintained. The latest version on record is v5.3.0, from 2025-01-07. Distribution utilities and researchers studying DER integration, demand response, and end-use load behavior on feeders.

Consider OpenDSS (with OpenDSSDirect.py / DSS-Python) (OpenDSS (Open Distribution System Simulator), EPRI's electric power distribution system simulator, usable from Python via the OpenDSSDirect.py and DSS-Python interfaces). It represents electric power distribution feeders and systems represented as buses, lines, transformers, capacitors, regulators, loads, PV systems, storage, and other distribution components, supporting unbalanced multi-phase steady-state and quasi-static time-series analysis. A user can change feeder topology and circuit element definitions (lines, transformers, loads), load shapes and time-series load/generation profiles, PV system and storage device parameters, regulator/capacitor control settings, and solution mode (snapshot, daily, yearly, fault study). In response, the model reports node/bus voltages per phase (and voltage profile), element currents and power flows, total circuit losses, per-element power and energy over time-series runs, and convergence and fault-study results. To run it you need Windows COM build or cross-platform DSS C-API; Python via pip install OpenDSSDirect.py or dss-python. It is permissively open source, released under the BSD 3-Clause (OpenDSSDirect.py and DSS-Python; OpenDSS core uses a BSD-style EPRI license) license. As of the last check it was actively maintained. The latest version on record is OpenDSS core version not pinned; DSS-Python 0.15.7, OpenDSSDirect.py 0.9.4, from OpenDSSDirect.py 0.9.4 released 2024-03-29; OpenDSS core repo updated 2026-01-30. Distribution engineers, DER (distributed energy resource) integration researchers, and utilities analyzing unbalanced feeders and grid modernization.

Consider OpenStudio (OpenStudio, NREL's open-source software development kit (SDK) for building energy modeling that wraps and orchestrates EnergyPlus (and Radiance)). It represents whole-building energy models (geometry, constructions, HVAC, loads, schedules) that are translated to and simulated by EnergyPlus, with workflows for measures, parametric studies, and results processing. A user can change building geometry and space/zone definitions, HVAC systems and plant loops via the model API, constructions, materials, and internal loads, OpenStudio Measures (scripted parametric modifications), and weather file and run period. In response, the model reports annual and timeseries energy use by end use, zone temperatures and comfort, heating/cooling loads and equipment sizing, utility cost and (via measures) emissions, and EnergyPlus error/warning and unmet-hour reports. To run it you need OpenStudio SDK/CLI (C++ with Ruby/Python bindings); bundled EnergyPlus engine. It is permissively open source, released under the BSD 3-Clause license. As of the last check it was actively maintained. The latest version on record is v3.11.0, from 2026-01-15. Building energy modelers and developers who want a scriptable API and measures workflow on top of EnergyPlus.

Consider OSeMOSYS (OSeMOSYS (Open Source Energy Modelling System), a long-term energy system optimization model generator for capacity expansion and energy planning). It represents national or regional energy systems represented as a network of technologies, fuels (energy carriers), and demands across time slices and years, used to compute least-cost investment and operation pathways. A user can change technology set, costs, and efficiencies, fuel/commodity definitions and resource limits, demand projections by year and time slice, emission limits and penalties, and discount rate and planning horizon. In response, the model reports optimal new capacity and total installed capacity by technology, energy production/use by technology and fuel, total discounted system cost, emissions trajectories, and dispatch across time slices. To run it you need GNU MathProg (GLPK) implementation, or Pyomo/PuLP/GAMS variants in Python; an LP/MILP solver. It is permissively open source, released under the Apache-2.0 license. As of the last check it was maintained, though at a slower pace. The latest version on record is no formal tagged release; repo updated 2025-10-22. Energy planners, governments, and researchers building national/regional least-cost energy transition pathways.

Consider pandapower (power system analysis tool in Python (built on the pandas data library) for steady-state modeling, power flow, and optimization of electric transmission and distribution networks). It represents electric power grids modeled as networks of buses, lines, transformers, loads, generators, switches, and other elements. Supports balanced and unbalanced steady-state analysis of transmission and distribution systems. A user can change network topology (buses, lines, transformers, switches), load active/reactive power (MW/MVAr) at each bus, generator/static generator setpoints and limits, line/transformer impedances and ratings, and solver/algorithm choice (Newton-Raphson, BFSW, etc.) and slack bus. In response, the model reports bus voltage magnitudes and angles, line/transformer loading percentage and power flows, active/reactive power losses, generator dispatch and slack injection, and optimal power flow objective and constraint results. To run it you need Python 3 (pip install pandapower); optional Julia/PowerModels.jl, lightsim2grid backends. It is permissively open source, released under the BSD 3-Clause license. As of the last check it was actively maintained. The latest version on record is 3.4.0, from 2026-02-09. Power systems researchers, distribution/transmission planners, and educators who want scriptable, automated grid analysis in Python.

Consider PowerModels.jl (PowerModels.jl, a Julia/JuMP package for steady-state power network optimization implementing many formulations of optimal power flow and related problems). It represents electric power transmission networks represented as buses, branches, generators, loads, and shunts, used to formulate and solve power flow and optimal power flow (and variants such as OTS, TNEP) under multiple mathematical relaxations/formulations. A user can change network data (buses, branches, generators) via MATPOWER/PSSE files, power flow formulation (AC, DC, SOC, QC relaxations), generator cost functions and limits, load/demand values, and solver choice (Ipopt, Gurobi, etc.) and problem type (OPF, PF, OTS). In response, the model reports optimal generator dispatch, bus voltage magnitudes and angles, branch power flows, objective (generation cost) value and solver status, and relaxation gap / feasibility metrics. To run it you need Julia (Pkg add PowerModels); JuMP and an optimization solver such as Ipopt. It is permissively open source, released under the BSD 3-Clause license. As of the last check it was actively maintained. The latest version on record is v0.21.6, from 2026-05-06. Power systems optimization researchers comparing OPF formulations and building custom transmission optimization problems.

Consider PyPSA (PyPSA (Python for Power System Analysis), an open toolbox for simulating and optimizing modern electric power and integrated energy systems). It represents electric power systems and sector-coupled energy systems represented as networks of buses, lines, links, transformers, generators, storage units, and loads, supporting power flow and linear optimal investment/dispatch (capacity expansion) over time series. A user can change network components (buses, lines, links, generators, storage), generator capacities, costs, and capacity-factor time series, load (demand) time series per bus, line/link capacities and reactances, and optimization objective, constraints, and CO2/emissions limits. In response, the model reports optimal generation/dispatch per generator over time, line/link power flows and loading, nodal/bus voltage angles (power flow) and marginal prices, storage state of charge over time, and total system cost and optimal capacities. To run it you need Python 3 (pip install pypsa); an LP/MILP solver (HiGHS, Gurobi, CPLEX, etc.). It is permissively open source, released under the MIT license. As of the last check it was actively maintained. The latest version on record is 1.2.2, from 2026-05-25. Energy system modelers and researchers doing capacity expansion, dispatch optimization, and sector coupling studies.

Consider PyPSA-Eur (PyPSA-Eur, an open optimization model of the European electricity and energy system built on PyPSA, with high spatial and temporal resolution). It represents the European transmission grid and energy system, modeled at the level of substations/regions across European countries, including conventional and renewable generation, storage, transmission lines, and (in the sector-coupled variant) heating, transport, and industry. A user can change spatial resolution (number of network clusters/nodes), temporal resolution (number of snapshots/hours), CO2 emission limits and cost assumptions, technology availability and capacity constraints (renewables, nuclear, etc.), and weather/renewable resource years and demand scenarios. In response, the model reports optimal generation and storage capacities by technology and region, hourly dispatch and cross-border power flows, total system cost and per-region marginal prices, CO2 emissions and renewable share, and transmission line expansion/loading. To run it you need Python 3 with Snakemake workflow (conda/mamba environment); an LP/MILP solver (HiGHS, Gurobi, etc.). It is permissively open source, released under the MIT (code; data under various open licenses) license. As of the last check it was actively maintained. The latest version on record is v2026.02.0, from 2026-02-19. Energy policy analysts and researchers studying European decarbonization, grid expansion, and sector coupling at scale.

Next are infrastructure networks for water. These models route flow and pressure through pipes, junctions, and pumps, and they are the public standard for utilities and consultants alike.

In this domain the inventory records 4 models.

Consider EPANET (EPANET, the US Environmental Protection Agency's model for simulating hydraulic and water-quality behavior in pressurized drinking-water distribution networks). It represents drinking-water distribution systems represented as networks of junctions (nodes), pipes, pumps, valves, tanks, and reservoirs, simulated for extended-period hydraulics and water quality (e.g. chlorine decay, water age). A user can change network topology (junctions, pipes, pumps, valves, tanks), pipe diameters, lengths, and roughness coefficients, nodal water demands and demand patterns, pump curves and control rules, and water-quality source concentrations and reaction coefficients. In response, the model reports node pressures and hydraulic heads, pipe flows and velocities, tank water levels over time, water age and chlorine/contaminant concentrations, and pump energy use and operating status. To run it you need EPANET Windows GUI, or the EPANET toolkit/library (C API) usable from Python (e.g. via WNTR or EPANET-Python). It is permissively open source, released under the public domain (US EPA); OWA EPANET engine under MIT license. As of the last check it was actively maintained. The latest version on record is v2.3.5 (OpenWaterAnalytics EPANET); EPA EPANET 2.2.x, from 2026-02-20. Water utilities, civil/environmental engineers, and researchers analyzing distribution-system hydraulics and water quality.

Consider pyswmm (pyswmm, a Python interface to the EPA SWMM5 (Storm Water Management Model) engine enabling scripted, step-by-step control and analysis of stormwater/sewer simulations). It represents urban drainage and sewer systems (the SWMM model of subcatchments, nodes, conduits, storage, pumps) driven programmatically from Python, allowing interactive/step-wise simulation and real-time control logic. A user can change SWMM input model (.inp) parameters loaded into the simulation, rainfall and inflow time series, node/link/subcatchment attributes set during the run, control logic applied step-by-step (e.g. pump/orifice settings), and simulation step size and reporting. In response, the model reports node depth, head, and flooding over time, link flow and capacity, subcatchment runoff, system routing/continuity statistics, and values queried at each simulation step for control. To run it you need Python 3 (pip install pyswmm); bundled SWMM5 engine. It is permissively open source, released under the BSD-2-Clause license. As of the last check it was actively maintained. The latest version on record is 2.1.0, from 2025-09-09. Stormwater engineers and researchers who need scripted, step-wise control of SWMM for real-time control, optimization, or coupling with other tools.

Consider SWMM (SWMM (Storm Water Management Model), the US Environmental Protection Agency's dynamic rainfall-runoff simulation model for stormwater, sanitary, and combined sewer systems). It represents urban drainage and sewer systems represented as subcatchments, junctions (nodes), conduits (links), storage units, pumps, weirs, and outfalls, simulating runoff generation and routing plus water quality during single events or long-term continuous periods. A user can change subcatchment area, imperviousness, and infiltration parameters, rainfall time series / design storms, conduit geometry, slope, and roughness, node/storage and outfall configuration, and pump curves, weirs, and control rules. In response, the model reports subcatchment runoff hydrographs, node depths, flooding, and surcharge, conduit flows and velocities over time, system inflow/outflow and continuity, and pollutant loads/concentrations (water quality). To run it you need EPA SWMM Windows GUI, or the SWMM5 computational engine (C) usable from Python via pyswmm. It is permissively open source, released under the public domain (US EPA); pyswmm wrapper under BSD-2-Clause license. As of the last check it was actively maintained. The latest version on record is 5.2.x (EPA SWMM; e.g. 5.2.3). Stormwater and wastewater engineers, municipalities, and researchers designing and analyzing urban drainage and sewer systems.

Consider WNTR (WNTR (Water Network Tool for Resilience), a Python package built on EPANET for simulating and analyzing the resilience of water distribution networks). It represents drinking-water distribution networks (the same junction/pipe/pump/tank model as EPANET) extended to simulate disruptive events such as pipe breaks, power outages, contamination, and earthquakes, and to compute resilience metrics. A user can change network model (imported EPANET .inp) topology and properties, disruptive events (pipe leaks/breaks, outages, fire flows), nodal demands and demand-driven vs. pressure-driven analysis, control rules and repair/recovery strategies, and simulation duration and water-quality parameters. In response, the model reports node pressures and demand satisfaction over time, pipe flows and velocities, tank levels and water age/quality, resilience metrics (e.g. water service availability, population impacted), and effects of leaks/outages on system performance. To run it you need Python 3 (pip install wntr); bundled EPANET engine. It is permissively open source, released under the Revised BSD (BSD 3-Clause) license. As of the last check it was actively maintained. The latest version on record is 1.4.0. Water utilities and resilience researchers assessing distribution-system performance under disruptions and planning mitigation.

Now consider robotics and multibody simulation. These tools describe a robot as a tree of links and joints and then simulate the physics, so you can test control and motion before touching hardware.

In this domain the inventory records 7 models.

Consider Drake (drake is a C++/Python toolbox for model-based design, simulation, and verification of robots, providing multibody dynamics, optimization, and systems modeling developed by MIT and Toyota Research Institute (TRI)). It represents models robots and dynamical systems as multibody plants (manipulators, mobile robots, humanoids) with rigid/deformable bodies, contact, and actuators, plus controllers and estimators composed in a systems framework for simulation and trajectory/optimization-based design. A user can change URDF/SDFormat model files and their link/joint parameters, Contact model and solver parameters (e.g., hydroelastic vs point contact), Controller gains and trajectory optimization constraints/costs, Initial conditions and actuation inputs, and Simulation time step and integrator settings. In response, the model reports State trajectories (positions, velocities) of the multibody system, Contact forces and constraint outputs, Optimization/trajectory solutions and feasibility, and Visualizations via Meshcat. To run it you need Python 3 (pip "drake") or C++; Linux/macOS. It is permissively open source, released under the BSD-3-Clause (with some third-party components under other licenses) license. As of the last check it was actively maintained. The latest version on record is 1.53.0, from 2026-05-15. Robotics and controls researchers needing rigorous multibody dynamics, contact modeling, and optimization-based design and verification.

Consider Gazebo (gazebo is an open-source 3D robotics simulator that models robots, sensors, and environments with physics, rendering, and sensor emulation for testing control and perception software). It represents simulates complete robotic systems and their environments, including multi-link robots (arms, mobile bases, drones, legged robots), actuators, and sensors (cameras, LiDAR, IMU), interacting with physics engines for rigid-body dynamics and contact. A user can change World/SDF model definitions (links, joints, inertias, collisions), Physics engine and parameters (gravity, step size, solver iterations), Sensor configurations (camera resolution, LiDAR range, noise), Joint commands / actuator force and velocity setpoints, and Environment objects, terrain, and lighting. In response, the model reports Robot pose, joint states, and trajectories over time, Simulated sensor streams (images, point clouds, IMU readings), Contact forces and collision events, and Rendered 3D scene and visualization plots. To run it you need Gazebo (Jetty current; Harmonic LTS) on Linux/macOS; commonly paired with ROS 2 via ros_gz. It is permissively open source, released under the Apache-2.0 license. As of the last check it was actively maintained. The latest version on record is Jetty, from 2025-09. Robotics researchers and engineers developing and testing navigation, manipulation, and perception in simulation before hardware deployment.

Consider MuJoCo (MuJoCo (Multi-Joint dynamics with Contact) is a fast physics engine for simulating articulated rigid-body systems with contacts, widely used in robotics and reinforcement learning, maintained by Google DeepMind). It represents simulates articulated mechanisms such as robot arms, humanoids, quadrupeds, and hands as bodies connected by joints with tendons, actuators, and rich contact dynamics, defined in MJCF or URDF models. A user can change MJCF/URDF model definitions (bodies, joints, geoms, inertias), Actuator control signals (torques, position/velocity targets), Contact and friction parameters (solref, solimp, friction coefficients), Simulation timestep and solver/integrator settings, and Initial qpos/qvel state. In response, the model reports Joint positions, velocities, and accelerations over time, Contact forces and constraint outputs, Sensor readings (defined sensors: accelerometer, force, etc.), and Rendered frames of the simulated scene. To run it you need Python 3 (pip "mujoco") or C; cross-platform. It is permissively open source, released under the Apache-2.0 license. As of the last check it was actively maintained. The latest version on record is 3.9.0, from 2026-05-27. Robotics and RL researchers needing fast, accurate contact-rich multibody simulation for control, learning, and benchmarking.

Consider Project Chrono (project Chrono is an open-source high-performance C++ library (with a Python module, PyChrono) for multiphysics and multibody dynamics simulation, covering rigid/flexible bodies, contact, vehicles, and terramechanics). It represents simulates complex mechanical systems such as ground vehicles, robots, granular terrain interaction, and mechanisms as large multibody assemblies with joints, contacts, and finite-element flexible parts. A user can change Body masses, inertias, and joint/constraint definitions, Contact and friction models and parameters, Actuator/motor inputs and applied loads, Solver and time-stepper settings (timestep, iterations), and Terrain/granular and FEA mesh parameters. In response, the model reports Body trajectories, positions, and velocities over time, Joint reaction and contact forces, Deformation/stress in flexible (FEA) components, and Visualizations and exported simulation data. To run it you need C++ build, or Python via PyChrono (conda package "pychrono"); cross-platform. It is permissively open source, released under the BSD-3-Clause license. As of the last check it was actively maintained. The latest version on record is 10.0.0, from 2026-04-07. Vehicle dynamics, robotics, and engineering researchers needing high-fidelity multibody and multiphysics simulation including terramechanics and FEA.

Consider PyBullet (PyBullet provides Python bindings to the Bullet physics engine for real-time rigid- and soft-body simulation of robots, used widely for robotics control and reinforcement learning). It represents simulates robots and mechanisms (manipulators, mobile robots, humanoids, grippers) loaded from URDF/SDF/MJCF as articulated multibody systems with collision, contact, and constraint dynamics, plus deformables. A user can change URDF/SDF/MJCF model files and their parameters, Joint control modes and setpoints (torque, position, velocity), Physics parameters (gravity, time step, solver iterations, friction, restitution), Initial poses and base position/orientation, and Applied external forces/torques. In response, the model reports Joint states (positions, velocities, reaction forces), Link/base poses and trajectories, Contact points and contact forces, and Synthetic camera images and depth/segmentation buffers. To run it you need Python 3 (pip "pybullet"); cross-platform. It is permissively open source, released under the zlib license. As of the last check it was maintained, though at a slower pace. The latest version on record is 3.2.7, from 2025-01-30. Robotics and RL researchers and educators wanting lightweight, scriptable physics simulation with easy URDF import.

Consider URDF Robot Descriptions (ROS) (unified Robot Description Format (URDF) is the XML model that defines a robot's links, joints, kinematics, and dynamics for the Robot Operating System (ROS) ecosystem, driving visualization, kinematics, and physics simulation). It represents describes the physical structure of a robot (manipulator arms, mobile bases, humanoids, quadrupeds) as a tree of rigid links connected by joints, with inertial, visual, and collision properties. Consumed by ROS tools (RViz, robot_state_publisher, MoveIt) and physics simulators to model and control the robot. A user can change Link geometry, mass, and inertia tensors, Joint types (revolute, prismatic, continuous, fixed) and axes, Joint limits (position, velocity, effort) and dynamics (damping, friction), Kinematic tree topology (parent/child link assignments, joint origins), and Materials, collision meshes, and sensor/plugin attachments (via xacro/Gazebo tags). In response, the model reports Forward kinematics / link transforms (TF frames), 3D visualization of the robot pose in RViz, Computed joint states and end-effector positions, Collision geometry and self-collision checks (via MoveIt), and Simulated dynamics when loaded into Gazebo or other physics engines. To run it you need ROS 2 (e.g., Kilted Kaiju or a current LTS) with urdf/xacro, robot_state_publisher, RViz; URDF files are also consumed by standalone simulators. It is permissively open source, released under the Apache-2.0 (ROS 2 core packages) license. As of the last check it was actively maintained. The latest version on record is ROS 2 Kilted Kaiju (rolling/Lyrical Luth in development), from 2025-05-23. Roboticists, controls engineers, students, and researchers needing a standard, tooling-rich way to describe and simulate robot kinematics and dynamics.

Consider Webots (webots is an open-source robot simulator providing a complete environment to model, program, and simulate robots, vehicles, and mechanical systems with physics and sensor emulation, developed by Cyberbotics). It represents simulates robots and mechatronic systems (wheeled and legged robots, manipulators, drones, vehicles) as articulated bodies with motors and sensors operating in 3D physics-based worlds, controllable via multiple programming languages and ROS 2. A user can change World file (.wbt) definitions: nodes, bodies, joints, physics, Motor/actuator commands and PID parameters in controllers, Sensor configurations (camera, LiDAR, distance sensors, GPS, IMU), Physics parameters (basicTimeStep, gravity, contact properties), and Environment objects, terrain, and robot placement. In response, the model reports Robot pose and joint/motor positions over time, Simulated sensor measurements (images, ranges, IMU), Contact and collision behavior, and 3D rendered scene and recorded movies. To run it you need Webots application (R2025a) on Windows/Linux/macOS; controllers in C/C++/Python/Java/MATLAB or via ROS 2. It is permissively open source, released under the Apache-2.0 license. As of the last check it was maintained, though at a slower pace. The latest version on record is R2025a, from 2025-02-04. Robotics educators, researchers, and developers wanting an integrated, multi-language simulator with a large built-in robot/sensor library.

We move to aerospace and space mission modeling. These tools cover flight dynamics in the atmosphere, spacecraft attitude and orbit in space, and the geometry of an aircraft before it is built.

In this domain the inventory records 5 models.

Consider Basilisk (basilisk is an open-source astrodynamics simulation framework for high-fidelity spacecraft dynamics, attitude, and flight software simulation). It represents models spacecraft as multi-body dynamical systems with orbital and rotational dynamics, environment effects, actuators (reaction wheels, thrusters), sensors, and onboard flight-software/control algorithms, enabling closed-loop guidance, navigation, and control (GNC) simulation. A user can change Spacecraft mass properties and initial orbital/attitude state, Actuator configuration (reaction wheels, thrusters) and parameters, Sensor models and noise settings, Flight-software/control algorithm modules and gains, and Environment and disturbance models (gravity, SRP, drag). In response, the model reports Attitude and angular-rate time histories, Orbital state/trajectory, Actuator commands and states (wheel speeds, thruster firings), Control errors and pointing performance, and Power/energy and sensor measurements. To run it you need Python 3 front end with C/C++ simulation modules; built from source (AVSLab/basilisk); optional Vizard visualization. It is permissively open source, released under the ISC license. As of the last check it was actively maintained. The latest version on record is v2.10.2, from 2026-05-08. Spacecraft GNC engineers and researchers building and testing attitude/orbit control and flight-software algorithms.

Consider JSBSim (JSBSim is an open-source, general-purpose object-oriented flight dynamics model (FDM) for simulating aircraft and other flight vehicles). It represents models the six-degree-of-freedom flight dynamics of an aircraft (or rocket/missile), computing forces and moments from aerodynamics, propulsion, landing gear, mass properties, and control systems to integrate the vehicle's motion through the atmosphere. Runs standalone or embedded in simulators such as FlightGear. A user can change Aircraft configuration file (aero, mass, geometry definition), Control inputs (throttle, elevator, aileron, rudder), Initial conditions (position, velocity, attitude), Engine/propulsion parameters, and Atmospheric and environment settings. In response, the model reports Position, velocity, and attitude time histories, Aerodynamic forces and moments, Engine thrust and fuel state, Accelerations and angular rates, and Control-surface and trim states. To run it you need C++ library with command-line tool; Python bindings via pip ("JSBSim"); reads XML aircraft/script files. It is open source under a weak-copyleft license, released under the LGPL-2.1-or-later license. As of the last check it was actively maintained. The latest version on record is v1.3.1, from 2026-05-17. Flight dynamics engineers, simulation developers, and researchers needing a scriptable, embeddable FDM.

Consider NASA GMAT (GMAT (General Mission Analysis Tool) is NASA's open-source software system for spacecraft mission design, trajectory optimization, and navigation across regimes from low Earth orbit to deep space). It represents models spacecraft missions and their orbital trajectories, including high-fidelity force modeling (gravity fields, drag, solar radiation pressure, third bodies), propulsive maneuvers, propagation, and optimization/targeting for mission design and operations. A user can change Spacecraft initial orbital state and physical properties, Force model configuration (gravity field, drag, SRP, point masses), Burn/maneuver and thruster definitions, Propagator settings and mission timeline/sequence, and Targeting/optimization variables and goals. In response, the model reports Propagated orbit/ephemeris and ground track, Orbital element time histories, Maneuver delta-V and fuel use, Targeting/optimization convergence and achieved goals, and 2D/3D trajectory visualization and reports. To run it you need GMAT desktop application (Windows/Linux/macOS) with GUI and scripting; optional Python/MATLAB interfaces. It is permissively open source, released under the Apache-2.0 license. As of the last check it was actively maintained. The latest version on record is R2026a, from 2026-05-06. Mission designers, flight dynamics engineers, and researchers at agencies, industry, and academia doing trajectory design and navigation.

Consider OpenVSP (OpenVSP (Open Vehicle Sketch Pad) is NASA's open-source parametric aircraft geometry tool with integrated analysis capabilities). It represents models aircraft (and other vehicle) outer-mold-line geometry parametrically, then supports analyses including aerodynamic predictions (VSPAERO vortex-lattice/panel solver), mass properties, wetted areas, and structural/parasite-drag estimation on the generated geometry. A user can change Geometry component parameters (wing span, chord, sweep, airfoil, fuselage shape), Component layout and configuration, Flight/analysis conditions (Mach, angle of attack, Reynolds number), VSPAERO solver settings (vortex lattice vs panel, wake iterations), and Mass and material/structural inputs. In response, the model reports Aerodynamic coefficients (lift, drag, moment) and polars, Wetted area and reference geometry, Mass properties / center of gravity, Parasite drag buildup, and 3D geometry and degenerate-geometry exports. To run it you need OpenVSP desktop application (Windows/Linux/macOS) with GUI, plus AngelScript and Python API; includes VSPAERO solver. It is permissively open source, released under the NASA Open Source Agreement (NOSA) v1.3 license. As of the last check it was actively maintained. The latest version on record is 3.50.3, from 2026-05-15. Aircraft conceptual designers and researchers needing rapid parametric geometry plus integrated aerodynamic and mass analysis.

Consider Orekit (orekit is an open-source, low-level space flight dynamics library written in Java, providing building blocks for orbit propagation, attitude, and mission analysis). It represents models spacecraft orbital and attitude dynamics, providing orbits, dates, frames, attitude laws, force models (gravity, drag, SRP, third bodies), numerical and analytical propagators, maneuvers, and orbit determination for space mission analysis. A user can change Initial orbit/state and epoch, Force model selection (gravity field, atmospheric drag, SRP, third bodies), Propagator type and integration settings, Attitude law and maneuver definitions, and Reference frames and time scales. In response, the model reports Propagated orbital states/ephemeris, Orbital element time histories, Spacecraft attitude over time, Event detections (eclipses, station visibility, node/apside crossings), and Orbit determination residuals and estimated parameters. To run it you need Java (JVM); available via Maven Central; usable from Java or via wrappers (e.g. Orekit Python wrapper). It is permissively open source, released under the Apache-2.0 license. As of the last check it was actively maintained. The latest version on record is 13.1.5, from 2026-05-02. Flight dynamics engineers and developers building custom space mission analysis, propagation, and orbit determination tools.

Here is wind and marine energy. These models capture the aeroelastic behavior of a wind turbine, the wakes that turbines create downstream of one another across a farm, and the response of a wave energy converter in the sea.

In this domain the inventory records 4 models.

Consider FLORIS (FLORIS (FLOw Redirection and Induction in Steady State) is NREL's open-source engineering wind farm wake model for predicting turbine wakes, power, and wake-steering control benefits). It represents models a wind farm as an array of turbines whose wakes interact, using steady-state engineering wake models to estimate per-turbine and farm-level power under given wind conditions and control settings (yaw-based wake steering). A user can change Turbine layout (positions, number of turbines), Wind conditions (speed, direction, turbulence intensity, wind rose), Turbine type/power-thrust curves, Yaw angles for wake steering, and Wake model selection and parameters (Gauss, Jensen, etc.). In response, the model reports Per-turbine power production, Total farm power / annual energy production (AEP), Wake velocity-deficit flow fields, and Turbine thrust and effective wind speeds. To run it you need Python 3 (pip "floris"); cross-platform. It is permissively open source, released under the BSD-3-Clause license. As of the last check it was actively maintained. The latest version on record is 4.6.4, from 2026-02-24. Wind farm designers, layout optimizers, and controls researchers studying wake steering and energy yield.

Consider OpenFAST (OpenFAST is NREL's open-source aeroelastic simulation tool for wind turbines, coupling aerodynamics, structural dynamics, hydrodynamics, and control to predict whole-turbine loads and response). It represents models a complete horizontal-axis wind turbine (and, via FAST.Farm, wind farms) as a coupled multiphysics system: aerodynamic rotor, flexible blades and tower, drivetrain, nacelle, controller, and (for offshore) floating/fixed substructure and mooring. A user can change Turbine geometry and structural properties (blades, tower, drivetrain), Inflow/wind conditions and turbulence, Aerodynamic model settings (BEM/OLAF, airfoil data), Controller parameters (pitch, torque, via DISCON/ROSCO), and Hydrodynamic and mooring properties (offshore). In response, the model reports Blade and tower loads, deflections, and stresses, Rotor speed, power, and thrust time series, Platform motions and mooring tensions (offshore), and Generator/drivetrain torque and electrical power. To run it you need OpenFAST executable (C/C++/Fortran), Linux/Windows/macOS; Python via pyOpenFAST/openfast_toolbox for I/O. It is permissively open source, released under the Apache-2.0 license. As of the last check it was actively maintained. The latest version on record is v5.0.0, from 2026-03-12. Wind energy researchers and engineers performing turbine load analysis, design certification studies, and controls development.

Consider WEC-Sim (WEC-Sim (Wave Energy Converter SIMulator) is an open-source time-domain code from NREL and Sandia for simulating the dynamics and power performance of wave energy converters in operational waves). It represents models a wave energy converter as a multibody system of floating bodies connected by joints, mooring, and power-take-off (PTO) mechanisms, subject to hydrodynamic wave forces, to predict motions and absorbed/electrical power. A user can change Body geometry and hydrodynamic coefficients (BEM data), Wave conditions (regular/irregular spectra, height, period), PTO type and parameters (damping, stiffness, control), Mooring and constraint/joint definitions, and Simulation time and solver settings. In response, the model reports Body motions (heave, surge, pitch) time series, PTO forces and absorbed/electrical power, Mooring and constraint forces, and Response amplitude and power matrices across sea states. To run it you need MATLAB/Simulink with Simscape Multibody (WEC-Sim runs as MATLAB/Simulink code); MATLAB is required. It is reached through a campus or employer license, released under the Apache-2.0 license. As of the last check it was actively maintained. The latest version on record is v7.0.0, from 2025-08-26. Marine/wave energy researchers and engineers evaluating WEC dynamics, PTO control, and power performance.

Consider WISDEM (WISDEM (Wind-Plant Integrated System Design and Engineering Model) is NREL's open-source Python framework for multidisciplinary systems engineering and cost analysis of wind turbines and plants). It represents models a complete wind turbine and plant as coupled engineering subsystems (rotor/blades, drivetrain, tower/structure, balance-of-system, costs) to perform integrated design optimization and levelized cost of energy analysis. A user can change Turbine geometry and design variables (blade chord/twist, tower dimensions), Material and structural properties, Drivetrain and generator configuration, Site/wind resource and plant layout assumptions, and Cost model and optimization objectives/constraints. In response, the model reports Component masses and turbine/plant capital cost, Annual energy production (AEP), Levelized cost of energy (LCOE), Structural performance metrics (loads, deflections, stresses), and Optimized design variable values. To run it you need Python 3 (built on OpenMDAO); installable via conda/pip; cross-platform. It is permissively open source, released under the Apache-2.0 license. As of the last check it was actively maintained. The latest version on record is v4.2.1, from 2026-04-19. Wind energy systems engineers and researchers performing turbine/plant design optimization and techno-economic analysis.

Now we reach process and chemical engineering. These simulators assemble a chemical plant from unit operations such as columns, reactors, and heat exchangers, and then solve for the streams that flow between them.

In this domain the inventory records 3 models.

Consider ChemSep (ChemSep is a column simulator for distillation, absorption, and extraction, combining equilibrium-stage and rate-based (nonequilibrium) models). It represents models multistage separation columns (distillation, absorption, stripping, extraction) as a cascade of equilibrium or rate-based stages with feeds, draws, condensers, and reboilers, against a database of ~400+ chemical components. A user can change Number of stages and feed/draw stage locations, Component selection and feed composition/flow/thermal state, Operating pressure and pressure profile, Reflux ratio, reboiler duty, or product specifications, Equilibrium vs nonequilibrium (rate-based) model choice, and Thermodynamic/property models (K-values, activity coefficients). In response, the model reports Stage-by-stage temperature, composition, and flow profiles, Product stream compositions and purities, Condenser and reboiler duties, Column internals/loading diagnostics, and McCabe-Thiele and profile plots. To run it you need ChemSep desktop application (Windows); also bundled with COCO simulator (CAPE-OPEN). It is free to use but closed source, though the exact license is unstated and needs direct verification. As of the last check it was maintained, though at a slower pace. Separation-process engineers, students, and educators studying distillation/absorption design.

Consider DWSIM (DWSIM (an open-source steady-state and dynamic chemical process simulator) builds flowsheets of unit operations and computes mass/energy balances and stream properties). It represents models a chemical or petrochemical process plant as a flowsheet of interconnected unit operations (reactors, distillation columns, heat exchangers, pumps, separators, etc.) with material and energy streams. Used for design and analysis of full process flowsheets. A user can change Feed stream composition, flow rate, temperature, and pressure, Choice of thermodynamic property package (e.g. Peng-Robinson, NRTL), Unit operation specifications (column reflux ratio, reactor conversion, heat duty), Recycle and connectivity topology of the flowsheet, and Reaction set definitions and kinetics. In response, the model reports Outlet stream compositions, flow rates, temperatures, pressures, Energy duties (reboiler/condenser/heater loads), Column stage profiles (temperature, composition per stage), Phase equilibrium and property tables, and Convergence/mass-energy balance reports. To run it you need DWSIM desktop application (Windows/Linux/macOS, .NET); also scriptable via Python/IronPython inside the app. It is open source under a copyleft license, released under the GPLv3 (desktop versions); DTL thermodynamics library is LGPLv3 license. As of the last check it was actively maintained. The latest version on record is v9.0.5, from 2025-10-03. Chemical/process engineers, students, and researchers needing a free full-featured process simulator.

Consider Tennessee Eastman Process (tep2py) (tep2py is a Python interface to the original Fortran Tennessee Eastman (TE) chemical-plant control benchmark, simulating a five-unit process with normal and fault scenarios). It represents models the Tennessee Eastman challenge chemical plant: a gas-phase reactor, product condenser, vapor-liquid separator, recycle compressor, and product stripper producing two liquid products from four gaseous reactants. A standard benchmark for process control and fault detection. A user can change Disturbance/fault number (21 predefined fault scenarios, IDV 1-20 plus none), Simulation duration and sampling interval, Which disturbances are active over time (disturbance matrix), and Random seed for measurement noise. In response, the model reports 41 measured process variables (XMEAS 1-41), 12 manipulated variable trajectories (XMV 1-12), Time-series of reactor/separator/stripper levels, pressures, temperatures, and Composition measurements of streams. To run it you need Python 3 with NumPy and a Fortran compiler (the Fortran TE source is compiled via f2py). It is permissively open source, released under the MIT license. As of the last check it was dormant but still usable. The latest activity on record is from 2021-09-28. Process-control, fault-detection, and machine-learning researchers needing a runnable, MATLAB-free TE simulator.

We turn to circuits and electronics. These are simulators in the S P I C E tradition, where S P I C E stands for the Simulation Program with Integrated Circuit Emphasis. You give them a netlist of components and they solve for the voltages and currents.

In this domain the inventory records 2 models.

Consider ngspice (ngspice (next-generation SPICE — Simulation Program with Integrated Circuit Emphasis) is an open-source mixed-signal circuit simulator that solves netlists for voltages and currents). It represents models electronic circuits described as SPICE netlists — interconnected resistors, capacitors, inductors, diodes, transistors, sources, and subcircuits — performing DC, AC, transient, noise, and mixed-signal analyses. A user can change Circuit topology and component values in the netlist, Analysis type and parameters (.dc, .ac, .tran, .noise sweeps), Device model parameters (transistor models, etc.), Source waveforms and stimulus, and Temperature and simulation options. In response, the model reports Node voltages and branch currents vs time/frequency, Transient, AC frequency-response, and DC sweep waveforms, Operating-point reports, Noise spectra, and Measured quantities (.measure outputs). To run it you need ngspice binary/CLI (Windows/Linux/macOS); also embeddable as a shared library. It is permissively open source, released under the Modified BSD (BSD-3-Clause), with some compatible licenses for parts license. As of the last check it was actively maintained. The latest version on record is ngspice-46, from 2026-03-29. Electronics engineers, students, and EDA tool developers needing a free SPICE engine.

Consider Xyce (xyce is Sandia National Laboratories' open-source, parallel, SPICE-compatible circuit simulator built for very large analog/mixed-signal circuits). It represents models electronic circuits (netlists of devices, sources, and subcircuits) like SPICE, but designed to solve extremely large circuits on parallel/HPC platforms for DC, AC, transient, and other analyses. A user can change Circuit netlist topology and component values, Analysis type and parameters (.dc, .ac, .tran sweeps), Device model cards and parameters, Source/stimulus definitions, and Parallel-solver and runtime options. In response, the model reports Node voltages and branch currents vs time/frequency, Transient and AC analysis waveforms (CSV/PRN/raw output), DC sweep results, Measured quantities (.measure), and Solver/performance diagnostics. To run it you need Xyce binary (Linux/macOS/Windows); supports MPI for parallel runs. It is open source under a copyleft license, released under the GPLv3 license. As of the last check it was actively maintained. The latest version on record is Release 7.10, from 2025-07-21. Engineers and researchers simulating very large circuits, especially needing HPC/parallel SPICE.

Next are architecture description languages with analysis. These languages let you model the architecture of a system and then run analyses on it, such as whether a set of tasks can be scheduled or whether a safety property holds.

In this domain the inventory records 2 models.

Consider Cheddar (an open-source real-time scheduling simulator and analyzer that takes task sets described in AADL (Architecture Analysis and Design Language) or its own language and computes schedulability, response times, and processor utilization). It represents real-time embedded systems described as processors/cores, software tasks (periodic, aperiodic, sporadic, multiframe), shared resources, and networks-on-chip, modeled in AADL or CheddarADL. A user can change task periods, capacities, deadlines, offsets, and jitters, scheduling policy (RM, DM, EDF, preemptive/non-preemptive, multiprocessor, hierarchical), number of processors/cores and architecture, shared-resource access protocols, and task activation patterns. In response, the model reports schedulability result (feasible / deadlines missed), worst/best/average task response times, processor utilization factor, preemption and context-switch counts, and resource blocking times and deadlock detection. To run it you need Cheddar desktop application (compiled for Linux/Windows/Solaris); integrates with OSATE2, TASTE, and Ocarina. It is open source under a copyleft license, released under the GPL license. As of the last check it was maintained, though at a slower pace. The latest version on record is 3.3, from 2023-09-25. Suits real-time systems engineers, students, and researchers prototyping scheduling policies; the scheduling-theory domain makes it more expert-oriented, though it is also used for teaching.

Consider OSATE (Open Source AADL Tool Environment) (an Eclipse-based toolset for the Architecture Analysis and Design Language (AADL) that runs quantitative analyses such as flow latency, scheduling, resource budgeting, bus load, weight, and fault/safety analysis on a system architecture model). It represents embedded and real-time hardware/software systems (e.g. avionics, automotive control) described in AADL as threads, processes, processors, buses, memory, devices, and their bindings and flows. A user can change thread periods, execution times, and dispatch protocols, processor/bus binding of software components, flow specifications and end-to-end flow latencies, component weight and resource (memory, MIPS) properties, and error model / fault propagation definitions. In response, the model reports end-to-end flow latency results, scheduling feasibility / processor utilization, resource budget (memory, bandwidth, MIPS) surplus or overflow, bus load utilization, and system weight roll-up and fault-tree / safety analysis results. To run it you need OSATE 2 (Eclipse-based desktop application, Java); analysis plugins built in. It is open source under a weak-copyleft license, released under the EPL-2.0 license. As of the last check it was actively maintained. The latest version on record is 2.18.0, from 2026-03-05. Suits embedded/real-time systems and safety engineers who need quantitative architecture analysis; AADL's property-rich language has a learning curve, so it is more expert-oriented than casual.

Now consider parametric spreadsheet models. A spreadsheet is a simple tool, but when its inputs drive a sequence of recomputed outputs across dozens of linked cells, it is a runnable system model in every sense that matters here.

In this domain the inventory records 3 models.

Consider AMSAT/IARU Satellite Link Budget Spreadsheet (A satellite radio link-budget Excel spreadsheet (the Jan King AMSAT/IARU workbook) that computes uplink/downlink signal-to-noise and link margin from RF system parameters). It represents models a satellite communications RF link as an end-to-end chain: transmitter power, antenna gains, free-space and atmospheric losses, receiver noise, and required signal-to-noise, for both uplink and downlink of an amateur/CubeSat-class spacecraft. A user can change Transmit power and transmitter/feed losses, Transmit and receive antenna gains, Frequency and slant range / orbit geometry, Receiver system noise temperature/figure, and Required Eb/No and data rate / modulation. In response, the model reports Free-space path loss, Received signal power / carrier-to-noise (C/N), Eb/No achieved, Link margin (dB) for uplink and downlink, and Pass/fail against required threshold. To run it you need Microsoft Excel (or LibreOffice Calc) — .xls workbook with formulas. It is permissively open source, though the exact license is unstated and needs direct verification. As of the last check it was dormant but still usable. The latest version on record is Rev 1, from 2003. CubeSat/amateur-satellite teams, RF/comms students designing ground-to-space links.

Consider Artemis CubeSat Kit Power Budget Spreadsheet (A CubeSat power/energy budget Google Sheet (University of Hawaii Artemis CubeSat Kit) that tracks energy generation, consumption, and battery state across mission modes). It represents models the electrical power system of a CubeSat: solar-array generation over an orbit, per-subsystem power draw across operating modes, and battery charge/discharge balance to verify the spacecraft can sustain operations. A user can change Per-subsystem/component power draw (voltage, current, peak/average), Mission mode timeline and duty cycles, Solar power generation profile over the orbit, Battery capacity and state-of-charge limits, and Orbital period / eclipse fraction. In response, the model reports Energy consumed per mode, Energy generated over the orbit, Running battery charge / state of charge, Net energy balance (positive/negative) over the cycle, and Whether power is sustainable across the mission profile. To run it you need Google Sheets (or Microsoft Excel after download) — formula workbook. It is permissively open source, though the exact license is unstated and needs direct verification. As of the last check it was of uncertain maintenance status. CubeSat student teams and educators sizing a spacecraft electrical power system.

Consider GREET Model (Excel) (GREET (Greenhouse gases, Regulated Emissions, and Energy use in Technologies) is Argonne National Laboratory's Excel life-cycle model of vehicle fuel pathways and energy/emissions). It represents models the full well-to-wheels life cycle of vehicle/fuel systems — fuel production pathways, vehicle operation, and the energy use and emissions across many feedstock-to-vehicle combinations — as a multidimensional Excel workbook with macros and a GUI. A user can change Vehicle/fuel pathway selection (e.g. gasoline ICE, EV, hydrogen, biofuel), Fuel-economy / vehicle efficiency assumptions, Feedstock and process energy mix / electricity grid mix, Analysis year and parameter scenarios, and Upstream process energy and emission factors. In response, the model reports Well-to-wheels energy use per mile/unit, Greenhouse-gas emissions (CO2-equivalent), Criteria/regulated pollutant emissions, Petroleum/fossil energy consumption, and Comparative results across pathways. To run it you need Microsoft Excel (with macros enabled); GUI packaged with the download. It is free to use but closed source, though the exact license is unstated and needs direct verification. As of the last check it was actively maintained. Transportation, energy, and life-cycle-analysis engineers and policy researchers.

We turn to computing models built on the design structure matrix, shortened to D S M (a square matrix that records which elements of a system depend on which others). The models that qualify here compute something from that matrix, such as how a change propagates, rather than merely displaying it.

In this domain the inventory records 2 models.

Consider DesignStructureMatrix.jl (A Julia package for Design Structure Matrix (DSM) analysis that clusters and sequences a dependency matrix to reveal modules and minimize feedback, with plotting). It represents the dependency/interaction structure of a complex system or process, encoded as a square DSM whose elements (components or tasks) and off-diagonal entries (dependencies) define the architecture or process flow. A user can change the input adjacency/dependency matrix, element labels, clustering algorithm parameters, and choice of operation (cluster vs sequence). In response, the model reports clustered (reordered) matrix grouping related elements, sequenced ordering that minimizes feedback/loops, reorganized element labels, and DSM heatmap-style plot. To run it you need Julia (uses Luxor.jl for rendering). It is permissively open source, released under the MIT license. As of the last check it was dormant but still usable. The latest version on record is 0.3.0, from 2022-01-25. Suits Julia users and researchers analyzing system/process architecture; lightweight and scriptable but not a polished GUI, so newcomers must write a little Julia.

Consider DSM Clustering Algorithm (Thebeau) (A MATLAB/Octave implementation of Ronnie Thebeau's Design Structure Matrix (DSM) clustering algorithm that reorganizes a dependency matrix into modules by minimizing a coordination-cost objective). It represents the dependency structure of a complex product or system, encoded as a square Design Structure Matrix where rows/columns are elements (components, parameters, or tasks) and off-diagonal entries are interactions. A user can change the input DSM (interaction matrix), cluster-cost / coordination-cost weighting parameters, bid power and other clustering algorithm parameters, and number of iterations / random seed. In response, the model reports clustered (reordered) DSM with module blocks, total coordination cost of the clustering, cluster membership assignments per element, and visual matrix plot of the clustered DSM. To run it you need MATLAB (also runs in GNU Octave). It is permissively open source, released under the MIT license. As of the last check it was dormant but still usable. The latest activity on record is from 2023-01-16. Suits researchers and students of system architecture/modularity comfortable with MATLAB/Octave; not a packaged GUI tool, so a newcomer must edit and run scripts.

Finally among the core clusters we reach heterogeneous and digital-twin platforms. These frameworks let different kinds of model run together, or couple separate simulators into a single co-simulation that can substitute for a real system.

In this domain the inventory records 5 models.

Consider FMI / FMU Standard (functional Mock-up Interface (FMI) — the open standard defining the Functional Mock-up Unit (FMU) container that makes simulation models exchangeable and co-simulable across tools). It represents FMI is a free, tool-independent standard (a Modelica Association Project) defining a container and interface—XML, binaries, and C code packaged as a ZIP (an FMU)—to exchange and co-simulate dynamic models of engineered systems. It is what lets heterogeneous models from different tools be wired into one runnable digital twin. Note this is a standard, not a tool. A user can change FMU input variables defined in the model description, Tunable parameters exposed by the FMU, Initial/start values, Co-simulation step size and master-algorithm settings, and Connections between FMUs in a co-simulation. In response, the model reports FMU output variable time-series, Internal state/derivative values exposed by the FMU, Event timings, and Co-simulation coupling signals between FMUs. To run it you need Any FMI-importing tool/library (e.g., FMPy, PyFMI, OMSimulator); the standard itself is consumed via such runtimes. It is permissively open source, released under the CC-BY-SA 4.0 (specification text); BSD 2-Clause (C headers and XML schema) license. As of the last check it was actively maintained. The latest version on record is 3.0.2. Anyone integrating models across tools into co-simulations or digital twins; tool developers implementing import/export.

Consider FMPy (python library to validate, simulate, and co-simulate Functional Mock-up Units (FMUs), including a GUI for running and plotting them). It represents FMPy is a free Python library (maintained under CATIA-Systems / Dassault Systemes) for loading, validating, simulating, and co-simulating FMI 1.0/2.0/3.0 FMUs. It executes heterogeneous tool-exported models and can connect them, serving as a runtime for FMU-based digital-twin and system models. A user can change FMU parameters and start values, Input signals/time tables fed to the FMU, Solver and step-size settings, Start/stop time of the simulation, and Output variable selection. In response, the model reports Time-series of FMU output variables (returned as arrays / plotted), Internal variable trajectories, Validation/diagnostic results for the FMU, and Co-simulation coupling results. To run it you need Python (pip install FMPy); optional GUI. It is permissively open source, released under the BSD 2-Clause license. As of the last check it was actively maintained. The latest version on record is 0.3.29, from 2026-03-24. Python users and engineers who need to run, validate, and script FMU simulations and co-simulations.

Consider OMSimulator (OpenModelica's FMI-based simulation and co-simulation tool for composing and running connected FMUs into heterogeneous system models). It represents OMSimulator (OpenModelica project) imports Functional Mock-up Units (FMUs) and connects them into composite system models, running model-exchange and co-simulation with master algorithms and solvers. It lets models from different tools be wired together into one runnable digital-twin-style system. A user can change FMU parameters and start values (system parameter bindings), Connections between FMUs in the composite system, Solver choice (e.g., CVODE) and step size, Simulation time span, and Signal filtering / output selection. In response, the model reports Time-series of FMU output variables, Coupling signals between connected FMUs, State and time event detection, and System-level result files for plotting. To run it you need OMSimulator (C/C++ with Python and Lua APIs); bundled with OpenModelica, also standalone. It is open source under a copyleft license, released under the OSMC Public License license. As of the last check it was actively maintained. The latest version on record is 2.1.3, from 2024-11-22. Engineers and researchers assembling multi-tool FMU co-simulations / digital twins, especially within the OpenModelica ecosystem.

Consider Ptolemy II (UC Berkeley open-source framework for modeling and simulating heterogeneous concurrent systems using multiple models of computation (actor-oriented design)). It represents ptolemy II models heterogeneous concurrent systems—cyber-physical and IoT systems—where actors communicate through ports and a pluggable "director" sets the model of computation (continuous, discrete-event, synchronous dataflow, finite-state, etc.). Different subsystems can run under different semantics within one hierarchical model. A user can change Actor parameters, Director / model-of-computation choice per composite, Model topology (actor wiring), Input data streams and source actors, and Simulation duration and solver settings. In response, the model reports Plotted signal time-series from plotter actors, Actor output token streams, State-machine transitions, and Event traces under discrete-event semantics. To run it you need Ptolemy II (Java; Vergil graphical editor), JVM required. It is permissively open source, though the exact license is unstated and needs direct verification. As of the last check it was dormant but still usable. The latest version on record is 11.0.1, from 2018-06-18. Researchers and students studying heterogeneous models of computation, cyber-physical and dataflow system design.

Consider PyFMI (python package for loading and interacting with FMI-compliant models (FMUs), including simulation and co-simulation master algorithms). It represents PyFMI (Modelon community) is a Python package for working with dynamic models compliant with the Functional Mock-up Interface—loading FMUs, simulating them, and running co-simulation master algorithms to couple multiple FMUs into a runnable heterogeneous system. A user can change FMU parameters and start values, Input trajectories supplied to the FMU, Solver options and step size (via Assimulo integration), Simulation start/final time, and Master-algorithm coupling options for multiple FMUs. In response, the model reports Output and state variable time-series from simulation results, Coupled signals across connected FMUs, Diagnostics from the simulation run, and Result objects for plotting/analysis. To run it you need Python (pip/conda install PyFMI); depends on Assimulo for solvers. It is open source under a copyleft license, released under the LGPL v3 license. As of the last check it was actively maintained. The latest version on record is 2.21.0, from 2025-03-19. Python users coupling FMUs into co-simulations, especially those using Assimulo solvers.

We include a brief, adjacent note on systems biology. The Systems Biology Markup Language, shortened to S B M L, lets researchers share runnable models of biochemical networks. We include only those that read as a runnable system of many interacting parts.

In this domain the inventory records one model.

Consider Kholodenko1999 EGFR Signaling (BIOMD0000000048) (A curated BioModels SBML (Systems Biology Markup Language) kinetic model of epidermal growth factor receptor (EGFR) signaling, runnable as a system of ODEs). It represents models the short-term intracellular signal-transduction network downstream of the EGF receptor — receptor dimerization, phosphorylation, and recruitment of adaptor proteins (Grb2, Shc, SOS, PLC-gamma) — as a network of biochemical species and reactions, framed as a runnable multi-element biochemical system. A user can change Initial EGF / receptor concentrations, Kinetic rate constants for reactions, Total protein/adaptor concentrations, Simulation duration and time resolution, and Solver/analysis type (time-course, steady state, parameter scan). In response, the model reports Time-courses of phosphorylated receptor and complexes, Concentrations of Grb2/Shc/SOS/PLC-gamma complexes over time, Steady-state species levels, Response sensitivity to parameter changes, and Dose-response to EGF input. To run it you need An SBML simulator such as COPASI, libRoadRunner/Tellurium (Python), or JWS Online. It is permissively open source, released under the CC0 (BioModels curated models released to public domain) license. As of the last check it was maintained, though at a slower pace. Systems-biology researchers, modelers, and students learning SBML simulation.

We close with another adjacent note, on continuum solvers such as computational fluid dynamics, shortened to C F D. We include these only where a setup reads as a runnable, multi-element engineered system rather than a single physics demo.

In this domain the inventory records one model.

Consider OpenFOAM (openfoam.com / OpenCFD) (OpenFOAM is an open-source computational fluid dynamics (CFD) toolbox; a representative tutorial (e.g. the pitzDaily backward-facing-step flow) is a runnable meshed flow model). It represents models continuum fluid flow in engineered geometries by discretizing the domain into a finite-volume mesh and solving the Navier-Stokes equations. A representative engineered case is flow over a backward-facing step (pitzDaily), a canonical separated-flow geometry. A user can change Inlet velocity / boundary conditions, Fluid properties (viscosity, density), Turbulence model selection (k-epsilon, k-omega SST, etc.), Mesh resolution / refinement (blockMesh, snappyHexMesh), Solver and numerical scheme settings (fvSolution, fvSchemes), and Simulation end time / time step. In response, the model reports Velocity and pressure fields over the mesh, Turbulence quantities (k, epsilon/omega) fields, Recirculation/reattachment behind the step, Residual convergence history, and Field data viewable in ParaView. To run it you need OpenFOAM (Linux/macOS/Windows via WSL/containers); post-processing in ParaView. It is open source under a copyleft license, released under the GPL (GNU General Public License) license. As of the last check it was actively maintained. The latest version on record is v2512, from 2025-12-22. CFD engineers, researchers, and students simulating fluid-flow systems.

That completes the inventory. The facts above are kept deliberately parallel from one model to the next, so the same questions are answered for each. What does it represent. What can you change. What can you observe. What does it cost to reach. How well is it maintained. Where a fact could not be confirmed, the catalog says so plainly rather than guessing.
